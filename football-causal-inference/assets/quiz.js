document.querySelectorAll('[data-quiz]').forEach((quiz)=>{const button=quiz.querySelector('button');const feedback=quiz.querySelector('.feedback');button.addEventListener('click',()=>{const picked=quiz.querySelector('input:checked');if(!picked){feedback.textContent='Choose an answer first.';feedback.className='feedback no';return}const ok=picked.value===quiz.dataset.answer;feedback.textContent=ok?quiz.dataset.correct:quiz.dataset.incorrect;feedback.className=`feedback ${ok?'ok':'no'}`})});
