---
title: "Stanford CS329A Self-Improving AI Agents | Part 2 | Test-Time Compute Scaling"
source: "https://www.youtube.com/watch?v=-Ggc37xLj_Y"
author:
  - "[[Stanford Online]]"
published: 2026-08-03
created: 2026-08-21
description: "Want to dive deeper? This curriculum is covered in the following online courses: - Agentic AI professional education program: https://learn.stanford.edu/agentic-ai-2026.html - XCS329 graduate course: "
tags:
  - "clippings"
  - "cs329a"
  - "agents"
---

So today we are going to talk about inference scaling. We briefly hinted on this in the first lecture. So there are three stages for LLM kind of development. The first one is pre-training. This stage historically takes a lot more time. It could take several months, a whole lot of GPUs that are used for training these. And then there's the fine tuning step. Usually this is a less or historically has been a less kind of compute intense process. If you're talking about trillions of data tokens in a pre-training side on the fine tuning side, this is orders of magnitude less data that we use for fine tuning. And then there is the inference part where we get to use the model and we are going to see different ways that at inference time, the model be better and become more useful without changing the parameters of the model and without any kind of fine tuning of the models.

So let's first start with the large language monkeys paper. Again, you are familiar with the infinite monkey theorem by now because we discussed it on Monday. But to recap, the idea of this paper is that we have an LLM have a problem as input. Instead of generating the response for this problem only once, we are going to repeatedly ask the same input problem again and again. This could be a number of times. It could be 10 times, 100 times and so on. And we will see how that affects the performance. And then we are going to have a verifier that can tell us which one of these responses were the correct response. And then that is going to be the output of this system.

The reason we like that is that by doing this repeated sampling, we can make models that are inferior, for example, Llama 3 8B or 70B that is not as capable as GPT-4o with single attempt by doing this repeated sampling and selecting the correct one among the generated candidates. We can significantly improve the performance of these models and make them better than these larger and proprietary ones. And these are here, you can see examples across really hard math coding, different styles of math problem, question answering problems. Is this clear to everyone? It just seems like the models and smaller models already know the answers to these hard problems. And we are just like by doing this repeated sampling, we are kind of eliciting those and surfacing those answers. They just don't tell us that in the first trial.

Now this kind of paradigm of repeated sampling works well across pretty much any domain that we have tried so far, including agentic benchmarks such as SWE-bench, which kind of mimics like how a software engineer goes about editing code and creating new patches. So here we are seeing the coverage, meaning the fraction of problems that are solved by at least one of the samples. And here on the x-axis, we have the number of samples going from one to 1,000. And what we are seeing here is that a model such as DeepSeek can—I believe this was the DeepSeek V3 model—can outperform models such as Claude 3.5 or o1-preview after 1,000 samples. So it can solve more coding and software engineering problems than those. And the reason this is interesting is that we are doing, in this case, for coding problems, if we have unit tests that we can use to select which one of these samples is correct, we have an end to an automated way to create a more capable model out of this, in this case, out of DeepSeek and open source model.

Now, again, we saw pre-training scaling laws in the last class. And that was we can predictably kind of reduce the test loss given these equations in this paper. And these equations might change even the setup, but there is a predictable path to increase data, amount of compute use and the number of parameters in the model and use that to kind of predict or reduce the test loss. So this is for pre-training, but it turns out we can define a similar scaling laws, but this time for inference or test time compute. And that is something that we showed in this paper, basically we are showing that the relationship between coverage and the number of samples that are being drawn in parallel from the model follow an exponential power law. So as soon as this sees coverage, meaning how many problems are solved by at least one of the samples, and then k is the number of samples and A and B are these kind of coefficient that we can find through this, the curve-fitting parameters that we can find through this scaling kind of scaling behavior of the model.

Concretely, as we scaled the number of samples across a range of models from Llama 3, 8B, 70B, and models like Gemma or Phi, and this is for various range of parameter sizes going from 70 million parameters all the way to 70 billion parameters, we can see that we can predict this power law and have a predicted curve that closely for the most part very closely follows the actual practical kind of coverage that we are seeing as we increase the number of samples. So basically the way to think about it is that we can predict how many sample to achieve a certain coverage, how many samples we are going to need and how much resources we should allocate to achieve. And again these curves show that this behavior is true for a range of models and a range of parameter sizes. Again, I think one of the very interesting properties that we saw here is that for a very small model like a 70 million parameter, we also see this as scaling behavior. And of course this is true for much larger models as well. And it is true across domains.

Now we looked a bit more into why we see this kind of power law behavior in repeated sampling. Because it might be at first a little non-intuitive because if the probability of the pass at one being correct, the probability of the query is correct response to a question is for example P, then we can calculate the probability of at least one of the answers being correct at K for an individual problems. So for problem i, if we have the pass at one for problem i, we can calculate directly what is pass at K for problem i with this exponential equation for everybody, basically this one minus P is the probability of the first like K answers not being correct and then one minus that is the probability of at least one of these K answers being correct. So this is for an individual problem. But what we are seeing that across the suite of problems we are seeing the exponent, the power law scaling law, meaning across these problems we are seeing the equations are changing.

So the question that we wanted to answer is that how does this what kind of behavior should we see across the problem examples in the data set that justifies our exponential, sorry our power law scaling laws. And by looking into that further, what was observed is that in order to have the scaling laws that we observed there has to be that sufficient and necessary condition for it is that we have a long tail of hard problems. Basically from a mathematical perspective we are going to need that because this is the power law scaling, this is the per problem exponential scaling laws and this is like this kind of this sort, we need to have this sort of curve for the pass at one of problems in order to justify this power law scaling. And empirically when we look into the problems we saw that this condition holds.

And again, what this condition is is that we have a long tail of really hard problems on the most complex side of things. For example, let's take a look at this model and the count of the count of problems that are solved at pass at one and at more and more samples. It seems like across all these problems, a large portion of problems are simpler and they're solved in pass at one. And as we increase the complexity of the problems where the pass at one probability is going to become lower and lower, we have like a long tail of problems that kind of have that property. So these kind of curve that you see here from high at simpler problems and lower harder and harder problems, meaning lower and lower pass at one is the type of problems is the type of behavior that we are seeing across these tasks. And that is why we can see the scaling laws that we have observed.

Now, what this means for the science and the engineering around LLMs is that while previously companies would spend hundreds of millions of dollars or billions of dollars on pre-training alone and then a lot less on fine tuning and then almost like nothing at each inference call because it used to be like a one time back and forth between the user and query and the model. Now we have a new paradigm where we can spend a whole lot more compute on inference and use that to increase the capability of model. And this compute, the inference compute can be done offline. We can kind of release our agents to go solve a problem and just keep generating tokens and keep improving the quality of the answers that they generate.

Now, when we talk about this repeated sampling, something that's important to kind of have in mind is the need for automated verification because if you have a bunch of samples, you still need to know which one of them are the correct ones and how to go about selecting them. In certain domains, verification is easier than the others. For example, for certain types of math problems, you can use formal proofs. So given a proof, a strategy we can run them by these formal proof kind of software tools to verify whether each step is correct or not. We can write unit tests for coding and in some cases writing a unit test arguably is a much simpler task than writing the entire program that solves a problem. So the unit test can be written, for example, by humans and you can use that as our verifier.

There are other cases like AI as a compiler and this is one of the projects in the lab and the direction that I'm very excited about and that is when we can generate compiler like lower level code like CUDA for a source code like PyTorch with LLM. And the reason this is very viable is that, for example, here we can always compare the output of CUDA with the source code or PyTorch and see if they're the same for any given input. So we don't need, if we want to say whether the model has generated the correct CUDA code for our source PyTorch code, we can just see whether their outputs match for any inputs. For those of you who are not familiar for CUDA, I hope you all are, but it's like a lower level language for writing or hardware and optimize for hardware code on GPUs. For example, here we have a data set or benchmark on CUDA generation called KernelBench and here again you're seeing the same sort of linear improving coverage in this case for CUDA code generation as we increase the number of samples that we take from the model. And in this case, we have this again by default perfect verifier that we can use for measuring the correctness of our CUDA generated code. And there are other examples of this. Any basically any kind of translation between two languages if you want to like port from Python or C++ to Java or vice versa. Again, this is true. You can measure the equivalency much easier.

Now there are domains that we don't have verifier for and in those domains what we observe is that there is a large gap between best of n methods such as majority voting and model based rankers and what is the true coverage of the model. Let's take a look at one example here. So here we compare the few different methods. We are going to learn about reward models or verifiers on Monday more. But let's take a look at these examples. So we have a number of samples and then we are measuring the success rate here like basically what would be the output of this system. Majority voting or the green one is the we just see which answer among the generated answers has appeared the most. So based on that we just select that and answer that response as the output of our system. Does that make sense? It's just majority voting. And the blue line here is coverage. This is assuming we had a perfect selector of verifier in this case. And as you can see here there is a large difference between majority voting which plateaus after 10 or 50 samples and what we could possibly get if we had a perfect verifier. So this is a very very large gap.

And especially in this gap is even more pronounced if we have harder problems for example the MATH capital MATH here. Data set is a harder, more complex data set than GSM8K. And here we compared other methods like using a reward model and that generates that measures gives a reward per answer and we take the best of those. So based to combine these two we will learn more about these reward models. Basically they're LLM based kind of LLMs that are trained to kind of score their response quality. And even with those there is a large gap. And this is what we call the generation verification gap. So the generation it turns out we can generate a lot of good responses but the verification is this basically this gap tells us how much we can actually capture those responses or those correct answers.

The reason methods such as majority voting doesn't work is that as we look into the quality of answers it turns out that for some problems that are hardest to solve sometimes we see that they are solved maybe even once or two times or three times across the 1,000 or 10,000 samples that we take from the model. So these correct problems are actually very rarely solved by the model with the samples that we are taking from it. So it makes sense that a majority voting mechanism cannot capture those because they're rare. But for many of the problems in this case especially for the GSM8K which is simpler because the answers or responses are more simpler to generate majority voting works for many of the problems but even in this case for some of the hardest problems the frequency of correct answer is actually pretty low. So that makes it harder from a verification perspective. How do you find them? How do you find that which ones are correct?

Now I want you to hear some questions that some discussion questions that I want to put in here for like something like two minutes or so and I want you to think about it and hopefully we'll have some volunteers or I and volunteer some of you to answer some of them. So two minutes for this. Maybe we talk after. Okay let's get back. Who wants to answer question one? I'm very interested in how you would build on this if you were to do some research here or what takeaways you got on these papers. Anyone? Yes? Anything else? Yes? I already have a revise in a solution. By next round. Okay we're gonna hear about it in a minute. Sounds good, yes? And I will put that on. I will look at a hybrid approach where I will develop a homage dress along with a lot of advance but like documentation. And then a minute or two for the right actor and I do take this time and support this lesson on the accuracy and more or less rely on the original. Give me more accurate answers. That way you can increase the speed on the answer. So you're suggesting use RAG to boost or compare.

So I think something I've thought about also is just even if it's easy to medium level to do respect to verification or very popular debates, it would be cool to see sort of how asset can change with respect to K, when you have employees who are initial like almost like a self-study approach. If there's somewhere to maybe consolidate, let's let the model explore in this domain gather insights. You know, you're not asking it to sample solutions. And then you don't think it relates to some output context and we do that in front of the parallel sample. And you might see maybe better strengths with respect to that. Absolutely. And we're gonna learn about some of the ways to improve test time scaling beyond just repeating sampling. Including self-study search and tool use. Maybe one more. Yes. So I guess one interesting direction would be like trying to look at different domains, where there's a little bit of cool view. Right? And also maybe verification is not fully accurate. It means a bit mostly. Like verify it's its own model because of the situation.

Yeah, basically there are other ways to filter out the incorrect answers. Maybe use a simulation or some other tool use or another model to do that. So then all of the data set here, the 10,000 different samples per problem is available on on Hugging Face. If anybody is interested to go around how to kind of improve like to showing that generation verification that that could be a potentially a good research project for the for the course. Yes. Last question. Yes. So I'm wondering about this we were which is a work that we've done on on sampling, but like a week supervised on sampling of verifiers we don't have a thousand of them, but we can have 10 or 20 of them. But that direction is definitely also interesting. It's very costly though from a compute perspective. Maybe we should move on to then any any other like about the other two questions anything. I think we discussed some of it in our discussion by anything that you want to share last minute. Yes. That's what we've investigated whether the coverage number is. I felt like the task, the task cases but exactly. I'm not seeing it. Yeah, for example, for the math problems, we manually looked into the to the answer questions and it was something above like a 90s, like seven, eight percent by manually checking that. But that's also always a case for failure mode. If your unit test don't have to like coverage of the code. So the quality of the verifier matters.

Yes. Yeah. Let's talk about that. And that's how we can make different ways of test time scaling. So with the monkeys framework, we see we saw that repeated sampling is effective, but there are other ways to scale sampling. And let's take a look at this paper. Scaling LLM test time compute optimally can be more effective than scaling model parameters. We are going to touch on certain aspects of it that is most relevant to test time scaling. So there are two way like parallel sampling is one way for us to generate multiple answers per input question. But there is another way to generate or generate answers or use test time scaling to improve the quality. And that is when we do sequential revisions. For example, the model comes up with one initial steps of the problem or one initial approach for solving the problem and it keeps revising that or it keeps like improving and adding to that. And that is the sequential approach. So instead of asking the model multiple times, we let the model know that it can keep revising its answers, look into that from a different angles and continue doing so until it's confident that it's ready to generate an answer and then finally output that. Yes. Revisions sometimes in their own answers.

Now, there's another way to scale test time compute and that is how much how we select the answers. So for the parallel sampling, we looked into the outcome like there are n different responses we look into the outcome and see if they're correct or not. We mentioned unit tests and so on. But there is also this other way of learning base or outcome reward models where they look into the outcome and say whether they're correct or not. Of course, their accuracy or quality can be limited, especially if we go to new domains. But outcome base reward model is another way that they look into the final answer and they generate a score. Basically, these models are trained to give a reward based on an output. Then there is also the process reward models. So the difference between process and outcome is that process reward models are designed to give a score per step of the generated solution. For example, we have a math problems. There are like five steps that the model takes before generating a solution. Process reward models, again, we learn about this more on Monday, I believe, are trained to give a score for the steps of the generation. So that's the difference between and between the two.

Now for best of n sampling, with a verifier again, the problem is easy. We generate parallel samples and then we can use an outcome base reward model to see which one has the highest score and then take that as our answer. Then there are other ways that we can bring these reward models into the processing, into our test time scaling framework. For example, here, let's assume at each level of this answering this question, we can take force and we have a budget of four samples. We take these four samples and then here use a process reward model or PRM to kind of like, in this case, we select the top two with the highest score from our PRM. Then again, we sample from them and we continue. And we can have a threshold for selecting the top two responses in this case and expanding the tree from there. So here we are doing a beam search approach that uses the process reward model to guide how we are sampling our model. Is that clear?

Our model is trained to generate a score, imagine between zero and one, based on the step given the question or just the step alone. Is the PRM finding for the other model? Yes, PRMs are generally also points in primary language model. As you can imagine, they would work better for in domain kind of types of tasks. So if you have a certain task that you want to or of course training a PRM on a subset of that task helps you with test time scaling on that task better than a cross new task. But because they're language model based, they do exhibit some generalization across task as well. Yes? For the process reward, is this reward that's being computed in practice? I don't know what the research is like, for example, is it a token that is being done or maybe blocks a token so that it's being computed? Yes, it's not for a token, it's for like steps like imagine. And this is something we're gonna learn about in future lectures. Imagine these are like a math problem and then there are steps to answer that. Like in one way to do it is to have humans annotate the quality of each step like yes or no, this is good or bad. And then training the model based on each step, which could be like you can assume it's like one kind of intellectually meaningful chunk of the result or it could be you can define it as a sentence and so on.

And of course, as it's intuitive, as it's very intuitive, if you combine the two of outcome based reward model and parallel best of n and sequential revisions, you get better results. This field is still, there's a lot of unknowns kind of questions around this. But combining the two, for example, one simple way to kind of combine that is instead of one parallel, one sample and revisions, you do two and you use your, and you get two final answers and then you use the best one, either using your PRM or ORM to select how the two are. But you're augmenting each parallel sample with these sequential revisions and you can use a PRM to guide that search for each parallel, for each sequential revision. Something that's interesting is that the PRM, there are many off the shelf PRMs that you can use for your tasks. So you can go and say take a pre-trained PRM and use that to guide the search for your own task and you can potentially find a PRM that is most kind of relevant to the type of tasks that you're doing and you want to do test time scale. Or you can train your own PRM and ORM based on your own data as well.

So this paper looked into the math data set and something like 12K train set, 500 test questions, use a PaLM model. And what was interesting here is that they defined a notion of difficulty for computing, for measuring these different scaling behaviors. And for that, they looked into the pass at one performance of the model on each problem. And by just measuring that, they defined, okay, these are the problems that are hard, these are the problems based on like how many of the generated answers are correct across a body of a number of samples. They had like five different bins for the complexity of the problem and they did that as we see to kind of get some conclusion about what test time scaling works better for harder or easier task. They also trained their own process reward model and even they fine-tuned a model for creating these revisions, but right now the models, every model that we use pretty much, it's capable, it's just as long as it's instruction tuned, it's capable of doing revisions if you prompt the model to do so, ask the model to do so.

And there are some interesting observations here for example, here is the majority voting for these problems, the accuracy as the generation budget increases and then this is the, the, the purple one is the ORM one and the green one is the PRM one and then the blue one if you really try to optimize different ways of mixing and matching things. And this is, this area is the one that still is an open research like how do we do that? How do we go about mixing and matching revisions and parallel scaling to optimize the test time scaling because at the end of the day we want for each accuracy, we want the minimum generation budget to hit that accuracy. And in a few slides ahead, we talked about one method to optimize this scaling and budget performance per budget.

They also looked into these different bins of difficulties. So number five is the most difficult questions because the accuracy is the lowest and number one. And then here on the right, there's these colors is the sequential to parallel ratio. And while they're saying that okay in the easier, for the easier problems, it seems like this darkest kind of the darkest purple color achieves highest accuracy. And that's where we have a lot more more sequential kind of tokens compared to parallel. Then the problems are more difficult and to some extent those are some of the more interesting problems. This optimality is kind of harder to say. It's harder to say whether it's sequential or the ratio changes even from bucket four to bucket five on how you, how you should allocate that.

Another observation from this paper was that, and this is a still working progress and I believe still the true observation as of today is that for easy and medium questions, it seems like additional test time compute can be more favorable than scaling pre-training of the model. So here we have these two, like the easy are the green, medium is blue, and orange is the hard questions, it seems like if you look into the ratio of inference tokens to pre-training tokens, it's positive here. But as we get to, it's positive for the easy and medium problem, but for the hardest problem, it seems like larger models like a more pre-training or larger models still do better if you are from it from a tokens optimized perspective. And this is something that we have seen in many of our research in the lab or around the lab as well. It seems like models simpler, like open source models or smaller models are becoming more and more useful as we take more test time compute, apply more test time compute on them, but still for the very, very hard problem, still the frontier models which presumably use more to pre-training and they're larger. They do better, even if we had a whole lot like infinite budget for test time scaling. Of course, we don't have infinite budget, but any reasonably large test time scale. Yes.

So basically, yes, in pre-training is done once, but scaling is every time, right? Every time you run the model, but still, but still this is an interesting observation from the perspective of are we done doing pre-training because we can just keep sampling from the models we have or no, pre-training is still helpful and we should do more and more, right? Not everyone can pre-train a large model, right? So even if pre-training was an option for achieving was better than test time scaling in any scale, not everyone can do that because it's a very expensive process. So this is very interesting that for many problems, we can do test time scaling even if it's, we do it every time for a new problem because that's feasible. Also, on the other hand, for the hardest problems, we still need to do pre-training and better models still do better. Because it doesn't exist in that case, right? Specific to a specific task and just like compare like the common recipes that we use for training models. Let's just follow that and then see how much they do better at test time. Any other questions?

Okay. So here are maybe we discussed some of these already but here are some more questions that I give you another two minutes. Let me check the time. Yes, we do have time. To think about this, I think this helps you kind of like absorb some of the topics that we learned or heard about in the previous slides and then we discussed them in two minutes. Okay, should we start? So add any comments here or any questions you might have? Yes. Yes. That is when we discussed the beam search stuff and the PRM like bringing a PRM to kind of cut the tree at each level and explore only the more promising branches. Anything else? Yes. But also a discussion with one statement for you of thinking that these are common the more paths you have towards a success. So if you're solving it, therefore you require it to be more sequential. All of our problems, there are a few paths to solving that, so you would need to think about paths to find at least one, but a vital social. And we're sort of thinking about it also. So what you're doing is finishing the thoughts, in a way, where you are doing it in a sort of thought but very fine throughout the steps in the process that are proving that these branches are violating anything.

Yeah, so in terms of sequence, you're saying that for easier ones, easier problems, for harder problems, you may need more exploration, potentially, for easier. For easier problems, because like any kind of path might lead us to the correct answer. Why not need to explore different parallel solutions? Yeah, that is an intuitive kind of way of thinking about this. Any other thoughts? In general, this is a very interesting kind of problem to think about. Let's assume that these tokens that you generate is like a knob that you have to increase these problems, the quality of the responses. How do you allocate this knob? And how do you elicit these generations from the model?

So let's think about it. In the following, I'm going to talk about one way to optimize these kind of test time scaling to generate optimized answers. And that is the Archon paper, an architecture research framework for inference-time scaling. So the problem state and your TA is one of the co-authors of this work. So the problem in this paper is that, again, we are interested to see how we can mix and match different inference time scaling methods to build the kind of optimized frontier of capability or correctness versus cost. And the other thing that, again, all of these questions basically saying the same thing, how we can optimize assigning inference compute to two different tasks and basically design mechanisms that gets us to the high quality answers while not wasting too many tokens and generations.

So in this case, we thought of inference scaling in this project as an inference architecture design problem. So here is how the frame, Archon frame, works on the input side, we had a set of target benchmarks that we wanted to get good results on these benchmarks. So these are the benchmarks we are optimizing for. There's an inference call budget. And then there's also a set of available LLMs because we can always mix and match different models also to get to a correct answer for an input question. And then we have a set of inference time techniques, which I'm going to describe in the following slides. We designed this optimizer. It's called ITAS, it's an inference time architecture search that basically tells us how to put together these different models and different inference time techniques together, whether it's parallel sampling, sequential, and all that in order to get high quality results given the budget, the inference call budget that we have, how to optimize the quality of results based on that. And then the output is this architecture that mixes and matches these different techniques and models together.

So let's take a look at different inference time operations. So first we have the generation, we call it the generation, but this is basically just sampling from the model. And this can be part of the repeated sampling approach, right? So if we generate N, we use N generations at a time that means we have generated N different responses to our input question. So that's one type of inference time scaling. Another one is fusion, which was surprisingly a very effective method. So fusion is, let's say we have K, K different responses and we want to generate one output response out of this K. Fusion takes the LLM and asks an LLM the same exact question that I just said. Basically it says here's the input question, here are K different responses to this input question, given all of this generate or synthesize one output response for the input question. Basically we are showing all of the K generations to the model such that the LLM can generate an output answer while being aware of all the possible ways that this question is answered. And this is kind of, it goes under the category of sequential update, right? We have these parallel samples, but then you're kind of fusing them into one answer.

Then there is the critic, which basically for any given response, we can ask a model to generate or describe the weaknesses or strengths of these response. And that could be one kind of inference scaling action. And then we have the ranker. This is again, these are all prompting based optimization for the model. We don't have, we haven't trained the model to do this specific test time scaling task. We just asked the model in the instruction prompt that can you rank these generated responses from the previous step based on their quality. And then the verifiers generates the, returns the response with, while also talking about the reasoning of how, why it came to this kind of way to score a certain answer. I'll get to the next two, unit test generation and evaluation in a bit, but just to show you how effective these different inference time scaling techniques are, let's take a look at this win rate for what task. We don't have the task here, but it's one of the tasks of like one of these benchmarks that, I don't know if you remember the task. Yeah. So it's some problem, question answering reasoning benchmark.

So here let's take a look at how these things work. So first we have, let's say we have a number of repeated samples from the model. It goes from one to 10 on the left side. Let's focus on the graph on the left side. And then the orange one is the kind of worst possible way of approaching. Let me just randomly select one and look at the quality and the correctness of this response. The green one is when we use the model to rank them, to rank the responses and we select the top one ranked by the model and report that. That already does better than, that already does better than the orange one. The blue one is oracle selection, meaning let's assume we are not mixing these responses in any way. We just select the best one generated. Let's say we have an oracle verifier that can tell us which one of these is correct. And that obviously does better than just asking the model to rank them because it's an oracle verifier. And then we get to the red one, which is where we, when we use the fusion. So basically for each, each point here, let's say we are at five, the red data point is basically we have given these five responses to the model and ask the model to synthesize one answer with one final answer, given all this information. And it's so interesting and this paradigm is so powerful that that can on its own improve the quality of responses over oracle selection. And then finally, the purple one is where we, instead of fusing all the responses, we first selected the top five in the, and then, and then we went about fusing them. So it seems like some sort of filtering of the two does better.

On the right side, we are doing a different experiment and that is we are on the left side, we had only one model generating everything. On the right side, we were using, we were adding model. So first one is just one model generating one answer to the baseline is two different models is generating one answer and 10 is 10 different models, each generating one answer. And we see that, of course, the random result becomes worse because the model we looked on the left was, I think it was among the better models in this ensemble. But we still see the same trend, trend, even if these responses are coming from different models, like basically the same trend of how these all follow each other. Yes. The way we created the ensemble was first with the best model and then added the second best model and so on.

Okay. Now let's take a look at a couple other questions, time techniques. And that is unit test generation and evaluation. I think one of you was, we're mentioning something like this earlier as well that where we can ask the model to generate these unit tests, say when it gets to coding tasks or math, up to math, problem solving or other reasoning tasks. And the unit test evaluation is even crazier, like instead of like running the unit test, you're asking the model to evaluate a generated answer against a unit test. For example, here is a prompt check for, so the coding problem in this case is check for balances of round brackets in an input string. That's a coding question and then here is an example input. Here is the type of unit test that the model could generate. For example, given a string with an odd number of brackets, the solution should output no, which is like a good unit test in this case. Another one is when a closing bracket is encountered, it must match the most recently open brackets that hasn't been matched yet. That's another kind of reasonable unit test and then you can ask the model to actually generate the code to test this as well.

Now let's take a look at the types. I don't know if you guys see this well, but the type, but this is so big, there's no way for me to put it here with like large font. But this is the type of architectures that Archon found. So basically now, think about it this way, we have different ways of mixing and matching models and inference time techniques and we can define these layers of optimization. For example, the first layer in this case is generation and then these generations are from different models in this case. And then we have a critic and a ranker. So basically, the critic will criticize this generator and then we rank them and then the optimizer found that we could have a bunch of fuses at this point, that each fuses these responses into one output and then we can go from there, continue this critic ranker. And this was like a rather complicated architecture and the way that we optimize this architecture is that we did some kind of pre-processing to reduce the space of optimization, for example, the fact that we found this sequence of generation critic ranker and fuser do really well. We did that offline, so we limited the search space just because doing this search is very expensive, we need to run a lot of inference calls. But then the way we optimize it that we had a held out data set that we had a training data set that we could optimize against that and create this accuracy versus the number of calls that we are making to these models as a metric that we are optimizing against. We want to maximize accuracy for any given inference budget that we have.

And this is for some of the coding problems. This was an optimized architecture. Basically, we generate a ton of samples, we generate unit tests and then we evaluate them. And interesting property here is that we found that stacking more and more layers of these inference-time techniques or making it deep in a way actually does help with our accuracy. In this case, for example, the right most pink graph is where we have an ensemble, meaning we have different models in our loop and we have three layers of critiques and fusers and a final fuser layer. And this is significantly better across many of these tasks over both just using a simple—the best model only once or the best model eight times with one layer of fusion. So it seems like these additional layers that we are adding here is actually just like in deep learning, we are adding layers and pre-training and the model gets better. So all the kind of addition of these inference layers are helping the model become more and more accurate across many of the tasks. This is true.

And so now the optimizer itself, we used an Bayesian optimizer. Again, to simplify the search space, we did a few things. For example, we limited the optimizer to only use one inference-time technique per layer. And the first layer was always just a generator. And then we had this cascade of ranker critic verifier. They could go anywhere, but we made certain kind of modifications to the search space. For example, a critic should go before a ranker or a fuser always. Because we did some testing locally and saw this is a better choice. And then a unit test generator must be followed by an evaluator op. Bayesian optimizer to do this. This is often a very powerful methodology, a ton of good open source software to do Bayesian optimization for you. Basically, you have this search space that you want to configure it with different kind of choices for the architecture and the output side you have the accuracy and you want to optimize for that. And this does better than greedy search or random selection. For example, efficient in terms of number of configurations that Archon searches until it reaches a final kind of architecture. And we can define different objectives for it. For example, available models, inference-time, inference-called budget is one of them or some of them.

The thing that I want you to pay attention to in this graph is that this interesting property that Archon is at the end generating one response at the very end. So it's like you're optimizing pass at one at the very end. And what was interesting here is that while using just the open source models, we could match or exceed these close source models at the time, the frontier close source model at the time, by a large margin across many of these tasks. In terms of pass at one average pass at one improvement. And not only that, we could train Archon to be task-specific. So when we are doing the Bayesian optimize and we only look into a certain task and we test on that, or we could train Archon to be general purpose. So we wanted it to do that good across many to do well across many tasks. And even that the general purpose one does really, really well and better than the frontier models across these tasks. And that was like a very interesting observation that you can design these inference-time architectures that do well beyond the task or the limited tasks that they're trained of. So on average, in this case, we were outperforming GPT-4 or Claude 3.5 Sonnet in pass at one by an average of 14.1% across these instruction following reasoning and math and coding problems.

Here are some questions that I want you to think about, but since we're out of time, hopefully you think about it yourself and hope you have a happy weekend.
