---
title: "Stanford CS329A Self-Improving AI Agents | Part 3 | Robust Verification"
source: "https://www.youtube.com/watch?v=p7TdPUcPoik"
author:
  - "[[Stanford Online]]"
published: 2026-08-03
created: 2026-08-21
description: "Want to dive deeper? This curriculum is covered in the following online courses: - Agentic AI professional education program: https://learn.stanford.edu/agentic-ai-2026.html - XCS329 graduate course: "
tags:
  - "clippings"
  - "cs329a"
  - "agents"
---

So lecture three is about verification. In the last lecture, when we talked about inference time scaling and the generation verification gap, basically what we discussed was that language models seem to be able to, seem to know the answer to many of the hard questions. And especially with methods such as repeated sampling or other scaling test time techniques, they can generate one, but a question is how can we automatically select which answer is correct or guide the model throughout the process of answer generation. So that leads us to verification, which we are learning about today. So in today's lecture, we are going to learn about the following four papers. And it kind of there is a progression of like how our kind of the way we approach verification changed or progressed throughout the years.

So let's first start with training verifiers to solve math word problems. This is a paper from OpenAI in 2021. And the motivation for this work was that LLMs hallucinate and can confidently present wrong solutions to the users. This still is true to this day. Four years later, of course, the model has become significantly better. There is also at the time, one of the contributions of this paper was to introduce a new reasoning benchmark around math. And you can see here in blue one of these problems. This is the type of problem that at the time, it was hard for LLMs to solve. And this led to this new data set. And that has become kind of a big part of many of the benchmarking tasks for LLMs. And even to this day, this data is called GSM8K. And even to this date, for smaller models and benchmarking, it's still a very useful language modeling evaluation. And you can consider it for the type of evals that you want to define on your projects as well.

So it basically consists of 8,500 grade school math problems. And the goal was this for this was to have the quality and diversity of the problems. But also importantly, there was a focus on multi-step reasoning, meaning that to solve the problems, even though the problems were simple, but the model required a few steps of reasoning to get to the answer. And you could also collect the solution in natural language as opposed to pure math. But the other contribution of this paper, which we were going to spend more time on, was to train a verification model where the verifier outputs the probability that a solution is correct.

So let's see about how this can be useful. For example, the model has been as humans we solve problems. It would be very, very good for us to have access to kind of a rubric or a way for us to tell whether we are correct or not. And we kind of want to provide the same thing through this verifier for the language model. So now how these verifiers, the verifier was trained in this case, is that assume that we have a question and a solution for it. Then we do have a label, whether it's a binary label, whether it's correct or incorrect. And during the training process, the generator, the language model, which is in charge of answering questions here, produces SI, which is the solution to question QI. And then we can generate, do repeated sampling and generate multiple cases in this case, 100 solutions per problem. And then we create these labels based on whether they were correct or not. Remember that these questions and answers were selectively designed by a lot of manual human supervision. So we know what the final answers to these problems are. So we can just based on that, create these labels for these problems. And then given that we can train a verifier, because now we have a question, we have a solution, and then we have a label for it.

Now the way we use it at test time is that at test time again, we generate a lot of answers and then use the verifier and look at the score, basically the log probability that is in this model now to see which one has a higher score and use that for as our final answer. So in this paper, the verifier was trained not just based on the correctness prediction, but there was also a language modeling objective. So the language modeling objective is also like given, for example here, given these tokens corresponding to the questions. The moment we get to the solution, solution token, we also add a loss that kind of urges like motivates the model to predict or reduce the the solution kind of distance from the true labels for that solution. So in this paper, they said that it helped them to have two losses. One is the binary loss and the other one was the normal language modeling, next prediction token loss. And the verifier itself, the architecture of the verifier is also a language model. But also this language model has a small scalar that outputs the binary prediction on a per-token basis. So basically on a per-token, you're saying how close was the solution to the true labels generated by humans. And then the tokens and questions are masked out because we're not optimizing for that. So we generate the question, we pass in fill in the model with the questions and we ask it to generate the solutions and we have you work with these two losses.

The way they train the model was to fine-tune the generator language model for two epochs and then sample 100 completions per question, label it whether they were correct or not and then train a verifier for a single epoch and this data set. So first they did fine-tune the language model on that data set, on a portion of that data set. And this step maybe it's like debatable whether we still need this step or not because language models are already very good at instruction following and understanding math. So maybe and for a lot of newer verifiers we directly go to the training the verifier object without any supervised fine-tuning first.

So they did a number of ablation studies. One was to create this label of correct or incorrect at the sentence level. So we have a loss that only concerns at each sentence level. For example, after each period looks into the previous sentence and looks better, this is a correct step given the steps of the correct step for the solution or not. Or they could do at a token level. So this is like very, very noisy kind of label. Like basically we want every token to follow the pattern that leads to a final correct solution at the end of this generation. And then the way this is used at test time is the final solution score is the score after the last token. Because if you have a token label prediction per token, at the end of the day we want only one prediction for the entire generation whether it's correct or not. And it turns out in this case they looked it after the last token.

All right, so we were talking about if you have a token, a label per token at the end of the day we just we want one token. Yes or no, whether this answer was correct or not. And this is done in this case by just looking at the very last token. And if that was a correct then it's the entire answer is assumed correct and otherwise it's assumed incorrect. So we predict for as soon as we predict these labels and then we only look at the very last label for them to generate and allocate a label for the entire solution. So here is looking back at these problems. When we get to the solution you're looking at the token level prediction by the trained verifier. And if it's green, meaning the verifier has a high score and the red ones means a low score in this case. And in this case this was there were some lower scores in the middle but towards the end it became greener and greener and this was a correct prediction at the end. At the end the last very last stage is a green one and we assume the actual score and the verifier predictions are in this case are matching. In this other case the last verifier is red. It starts off well but it has a there's something that the mistake is made which is towards the end of this generation step and the actual score is negative and the verifier prediction is also negative.

So this is how basically this verifier is trained for GSM8K and then they did some ablations or comparisons with a baseline where we just directly just fine-tune on this data set fine-tune meaning with supervised fine-tune. So we just take the base model and the next token bird prediction or we do this verification stage where we generate a hundred samples at each generation and use the highest score of the verifier as the true as the delivered output by our system. So the idea what we can see here is that for both 6B and 175B models this one is a GPT-3 model I believe. For both of these verification seems to work better especially as the training set size for the verifier increases. So this the verifier model eventually outperforms the fine-tuning only approach. And vice versa if you have smaller data sets in this case less than a thousand for the 175B the verification is not helping much. I think it has a solid rate of also 6B verification of the smaller bits. That's on the right scale. Is that even a word? Okay I see.

Let me go here. So this is another result which is which is very interesting and some this is something that could potentially be a research project that you guys can look into. So what they did is that they tested two approaches. One having a larger generator and smaller verifier and two having a smaller generator and larger verifier. And what they saw was that the larger generator helps with a smaller verifier does better than vice versa. And to some extent this is intuitive right because if we assume generation is naturally or on an x like on average is a harder task than verification then this this might make sense. But like finding the peripheral optimal of like how these two can kind of be the sizes with respect to each other would be a very interesting research direction question especially right now as the base model or generators have improved a lot but and also we have access to a lot of verifiers on hugging face there is like there's a leaderboard of like verification verifier models or reward models that can be tested against. So again this was the main message from this this ablation study.

Another result that about test time scaling that they they're showing is that as they increase the number of completions per problem and then they use the verifier it seems like around 400 or so the verifier that's that's where they get a lot of benefit but after that they're not getting a lot of benefit. So if they sample 800 solutions and they use the verifier against that the verifier kind of fails to track what's best and what's not compared to the truth true kind of distribution of the of the solutions. So one question for you is that do you remember last time when we talked about majority voting what was the range that we were getting out of that like we show that majority voting although is useful to some extent as we increase the number of samples per query it fails after some point to track and that's the generation verification yeah does any does anyone remember that okay. So that was we saw that the failure happens after 10 to like we never got to 50 samples like even though the coverage goes up the the pass at one or basically how we can generate select the correct response fails for around 50 or less for the majority voting approach but here they're showing that up until 400 they can increase the accuracy and the verifier is still useful.

Yes but these but the but when they they have so they do have the true labels per problem and then when they are testing this verifier the verifier ranks these at each point say for 400 these 400 solutions they take the highest score and they compare that against ground truth and that's what they're reporting here whether that was correct or not. At test time and training time as well for the verifier but not for the generator. So for each problem the human has once generated an answer that's offline that's done once then you take the model generate a hundred solutions per problem and you compare each of the solutions against the ground truth which is generated only once by humans and you see whether they match or not that's how you create the label okay any questions from this paper yes.

Like why doesn't it stay constant okay so the question is why does it drop after eight after 400 it shouldn't be constant because we are not showing the coverage we are showing the output of this entire system where the verifier ranks these say at this point 800 different solutions and we are getting the max score of the verifier but the precision of the verifier drops like it's like if two solution are too close to each other but one is correct and incorrect at across 800 it can't like distinguish between the two as well as it could do for 400 right so so that's where we get the drop in accuracy and in reality when they're outputting their they created the system they said that we stop that they stop at it a hundred samples because that's where they get most of the gains anyway and that's where they're consistent. Three painted samples so parallel so same question you ask the model a hundred different times to solve that yes because this is trained on a larger data set and because the verifier they consider both sentence base and token base it still works because the data that the trainer first of all the verifier is a language model itself so it knows a lot about this space itself but also through this label training it has been trained to be able to distinguish between correct and incorrect yes so the question and let me repeat the questions because some one of you asked this before so the question is does it make sense to scale the verifier do tests I'm scaling on the verifier yes I'm gonna see that in the in the future slides yes.

Like between each of that like before like around like a thousand steps like a thousand like dream size great the finding actually there's better going to verify if does that happen again like if you scale like to higher the series is very fine tuning versus verification do they cross each other again yeah like if you were to actually if you do need that much data sorry for example or test I mean I think they would that if you have a ton of data like a whole lot of data then fine tuning and verification probably they're gonna be the same and the kind of converge to the same thing but the but the beauty of a verifier is that you are not fine tuning your base language model you're not making it like really custom to like one data set or one specific test so that base model the generator model remains general and then now you have a verifier that you can kind of lead the the language the base model okay.

Now let's go through this second paper another paper by opening eye let's verify step by step and so this was this came about a couple years like less than a two years after that first paper again problems have had the same challenges they hallucinated if they if they're solving a problem step by step and they make a step in a single in an s-step early on it can be railed the entire answer right so the solution is that they were proposing here is that they consider these two kind of approaches to reward modeling one is outcome based reward model and the other is process based reward model so the outcome based one like we saw earlier in the previous paper it generates a reward to the entire solution it's about the correctness of the solution of the entire solution whereas the process one assigns a different reward per step of the optimization problem and so let's take a look exactly how this training can be done for the ORM versus PRM so we have a math problem the generator instead of generating a hundred solutions like it right now we are only looking into a one solution and it has a bunch of steps step one two and all the way until we get to the final answer and then we have the ground truth so we can know whether the final answer is correct or not because we can match the final answer against the ground truth now with ORM we are done there because we that's how we assign the label for for the model but for PRM the way they did it in this paper was that they looked into they had human annotators go through the steps of the generation by the model and assign a score to each step so step one in this case was correct step two was correct and probably because the the maybe all of these steps were correct that's why we let it led to the final correct answer but basically they created this entire data set of human annotated step wise kind of correct or incorrect labels and the final reward for this step wise now again in this case could be kept what they propose is that could can be calculated as the product of the step wise rewards per step of the model so they basically train the model against this step wise annotations and then they use that they create steps like you can even ask the model to generate the answers to a solution step by step so the steps are clear they run this PRM against each of the steps they get a score they multiply them with each other they get a range of like what the the quality of the answer and they can decide whether they output the answer or try again.

So this is called process supervision obviously there is this credit assignment and then it's the more precise way of collecting data and assigning labels to different steps of the problem rather than just looking at the output answer a property here that's very important actually this is like a really can it could be a pitfall for a test time scaling is that with process supervision we can manage the false positives way better than we can do with just outcome supervision supervision why because model might hallucinate and this happens surprisingly model can hallucinate and get to a final correct answer while the process for it is like really wrong whereas with process supervision because we are kind of like we are supervising we are seeing all the steps and we have a score for it it's less likely that we get into that the mode where the steps are wrong but we are the final answer is all correct also it encourages interpretable reasoning and human endorsed process of solutions it's human endorsed again because humans have created the labels.

So this paper produced this data set prm 800k it's an open source data set of 800k step level labels so the way they did this was a generator model against a language they took a language model and they generate a large number of samples per problem in a stepwise matter manner and then they also did this trick up like because they wanted to collect higher quality stepwise labels they took into what they call convincing wrong kind of answers meaning that they gave higher priority to samples where the the final answers were correct but the intermediate steps were incorrect and this enabled them to be 2.6 times more data efficient just then just randomly selecting these samples and give it to humans so they did it iteratively so they had like a very you can have a step with a PRM by just asking an LLM to judge a step right you can have that so they iteratively upgraded the PRM by collecting this data and then training on the PRM for it and then increasing the quality of the data so and at each step the label was that was collected was either positive or negative or neutral.

For example let's take a look at this problem the denominator of a fraction is seven less than three times the numerator if the fraction is equal to two over five what is the numerator of the fraction and then there are these steps and the first step was labeled correct second was correct and all the way to the fifth step was correct but this last step obviously the model failed to do the math x should be 15 14 but is seven so that was labeled incorrect so that's how they collected the labels here is the process so they took GPT-4 as their base model they still fine-tuned GPT-4 for both the ORM and PRM training on this data set first so this supervised fine-tuning first and for ORM of course they just have the pairs of generated samples and final correctness but for PRM they had this generators generated the sample and then the stepwise correctness label for ORM the final score was the score of the final token in the completion again they again went to the token level approach for ORM but for PRM it was the product of probabilities for every step.

Now let's take a look at some of the solutions they were showing that PRM outperforms ORM and majority voting so again here and is the number of solutions per problem majority voting again fails after in this case a hundred or so samples the blue line is the ORM model and the orange one is the PRM approach that is doing better and what they noticed that PRM detects correct solutions for some of the very rare kind of occurrences of a correct answer some problems with less than five percent correct answers in the distribution of samples they also showed the benefit of larger data in this active learning approach that they had again and the other kind of observation that they were showing is that PRM seems to be more data efficient so the number of labels that they collected for PRM if they match that against ORM both PRM and ORM benefit from more and more labels but it turns out that the PRM is more sample efficient right so so if they have a hundred solutions labeled labeled per problem for an ORM model that's kind of like having like one solution per problem based on the PRM approach and so on.

The other interesting property and that's what what we all should strive as we generate as we are training these kind of verify and reward model is the general generalizability to new domains and new data sets here what they are showing is that majority voting actually is better than ORM in terms of how it generalizes but PRM is a better is a better better than majority voting overall and it can tolerate a whole lot more distribution shift than majority voting and so overall these things like a much better approach here any questions yes so PRM is obviously could be to like false for an assignment if like a step looks good this doesn't actually give you the final answer or narrative means that you expect PRM is actually hurt so the question is can PRM hurt sometimes because the final answer might still be correct so the truth is like a lot of the newer approaches combined the two like the reality is they combine both the PRM and ORM based solutions because you want to get the benefit of both approaches and the other thing is that like the threshold that you can define for for PRM then is also like a hyper parameter that now you have introduced that you have to like deal with and optimize for your system and that that's an additional complexity here but yes and they did they did manage to like have because it's true ORM needs like K labels per problems but PRM needs K times some number of steps but then it's very hard to control that number of steps of like how do you exactly control but they did try to manage that and I believe some of the graphs they do that but that's a good point yes majority voting mainly in this complex so majority voting you don't have any access to any reward model you just see which sample or solution is repeated most and you take that as your final answer because they were talking about generalization this should be the same one yes yes yeah this one what if like the reasoning is let's call the numerator X and then X equals to 14 and it just means yes how does that avoid that very well happy so you're saying the PRM can give a high score what if it's like skip all the reasoning that's called the numerator X and then X equals to 14 and how does that avoid that kind of I think sorry I don't get it can you like the the reasoning that you just be numerator X and then X equals to 14 and how does that like the PRM avoid that kind of like real pattern scenario previous step here is the current score it yeah right so if X is 14 and it's great right it doesn't no no it doesn't but you can put the model to to do this because you're not changing the model right you're not changing your generator you can't like that failure mode could be when you were fine to new generator model then the model kind of talk reasoning and just generates and final answer that the PRM likes right but you're not catching your generator the generators can still be prompted to be reasoning create these steps and then the very fire just scores them if let's say we want to use the PRM to train the you have to be careful then you have to like make sure that the chain of thought makes sense right and your PRM again your PRM can the way you train your PRM can also be aware of that because your PRM could be as your your PRM these are like human generated labels in this case right if we had like skipped all of this and then we would go to S14 probably the human says this doesn't make sense this step is lower because this step is skipping if you kind of reasoning steps does that make sense so these labels are created by humans here so if the labels are not generated by human and we're going to see that in the next paper yes that is definitely a caveat like and something that you can make sure but there are ways to handle that as well okay.

So let's go through the next paper match shepherd verify and reinforce all of them step by step without human annotations so we're getting to what if we didn't want to collect that many annotations from humans so again motivation is that having stronger way fires shows the potential to really improve model reasoning behavior especially if you do test arm scaling PRMs have shown more potential than ORMs but they required a lot of data and it's hard to collect these data so can be automate the process of label collection for PRMs so this paper talks about two kind of approaches one is automatic annotation and also kind of brings the reward model as a into the loop of generator optimization via a reinforcement learning approach which we are getting briefly mentioned that so in this case the way they try to go about this automatic kind of annotation of steps was that they defined the quality of the reasons step as the potential of this step to reach the final correct answer so let's say we have we are at a given step the sample and kind of completions from that given step and see the potential for a correct solution at the very end so and they could introduce two ways of doing this one is the heart estimate where the step is annotated as a success if any of those end generations after that step gets to a final correct answer the soft estimate thinks of measures the frequency like what portion of the generations after a current step reached the final correct answer.

Here is like how it works say here is the problem and this is our current state instead of just sampling one step the sample n is three in this case so we sample three steps and then we continue and and we measure the frequency in the soft estimate we measure the frequency of correct answer at the very end in heart estimate we see whether any any of these are correct or not so heart estimate would be one for step one soft estimate would be two-third for step one anyone can kind of name it a challenge or a drawback of this approach so basically this this soft or hard kind of estimation is replacing the human annotations so we're getting this core per step by just rolling out more samples from that step and and seeing whether we reach to the final correct answer or not and assume that we do have final labels for these problems yes so I speak up with any server if it's unpredictable like a hundred and equal a hundred to see that final correct kind of like trajectory to score this step but we won't see that if you just look into three right and then we give a score of zero anything else yes samples you don't get any signal that's our hard problem in this case that's right exactly I think this is like probably like these two that you all mentioned here one just for hard problems you don't get signal the other one is what if there are like wrong steps in the middle and we got to the final current solution we still label those steps as correct so the focus that if you have more samples it shows the wrongness in one of these kind of trajectories but it's not guaranteed.

So here is the verification process you sample and again sample in candidate solutions score them using the PRM and you select the highest kind of PRM score across the generated samples they also took this PRM and trained the model with this PRM to encourage the model to generate steps that are scored higher and higher by PRM so PRM basically served as the reward model for this so they look into the heart versus soft annotations and although the loss function as they increased and would be it seems like the the soft annotations were doing better but somehow what they observed with that but then n equal 4 the depth of like the trajectory completions to collect these labels that's where they get like their best results and even so it really didn't matter whether they used the heart estimate or self estimate and they went with the heart estimate at the very end because it was just easier to to measure that.

So here is another comparison with baseline so here they have SCs major self-consistencies another name for majority voting which which solution was most consistent that's the red one ORM is the blue one and the shepherd is this PRM approach is the green one and the interesting part here was that there was no human annotation all the annotations were model generated or the mechanism that they introduced here they also compare with PRM 800K which was the very fire that we that in the previous paper that was trained and it outperformed that as well on math which is like a harder much harder set of this data set is more difficult than GSM 8K and they were getting even higher delta in there they also compare these results with different types of theifiers again across GSM 8K and math 500 problems and you can see that the difference between these models and like these approaches and there is a notable difference between these models and the baseline which is self-consistency approaches and this is true across different models 70g lemma 34b and deep-sick 67b showed that they can get even better they can make the model even better if they are all the model against the PRMs that they trained so in this case the match shepherd on this their their approach basically miss taking Mr. Mitchell 7b this model and then they PPO it against this PRM they're getting much higher results compared to doing this RL with just an ORM delta here is slightly less I would say than just the test time scaling but this is also another way for the models to self-improve right you generate the annotation by the model itself you train the PRM and then use that PRM to improve the the generator model as they're both at test time and during with this RL fine-tuning so it's like multiple levels of using the model itself to generate data and like RL reward signal for for itself yeah and then they can do RL and PPO and do verification up on top and they get even better results but it seems like kind of their optimizations are plateauing at some point any questions from this paper yes self air operation which I imagine be like desirable thing but looking for only rewarding each step be correct so encourage PRMs to do more self correction yeah yes absolutely like for example one of the methods that can be done is that you have a rubric or a set of rules that you give the model and as you're scoring a step so the model can check the step against this rubric that you're designed for this data set and that can you be used as extra signal whether this step makes sense or not right for example the very or like use a calculator in that case in the problem in the equation that we are saying if the model could use could do tool use or use a calculator in that case it would not I could solve that problem easily right could for example use a SIM pi so so there are ways to bring more supervision and more signal and enable the model to use tools and test time scaling and agent attack approaches to improve the verification process yes but the reasoning model like we're gonna talk about them in this class but basically what they're doing a big part of it is to generate these reasoning steps use the reward for it and do an RL loop and generate positive trajectories and then use that positive trajectories as part of training for the next round right so the test time scaling is inherently used to generate data and generate the training process but they still would benefit because they still could explore different parts of the solution space with with more sampling yes so the question is that would be converged to a future where this kind of test time scaling is just for training time to create the data and then at test time we we just used as the model ones I think that's the direction we want to go right we want the model to be really really good at pass at one like the first time we ask it to do something we wanted to generate and that increases the efficiency and all that like that would be the hope one challenge though with making the model to like like to make the the log problems of the model too sharp and true true one answer is that the creativity and diversity of solutions may be lost some somehow during the training so surprisingly we still like them like models that are very like create have a lot of creativity and diversity in the way that they they think and that's like a good feature that we want to have in the models yes does it matter if janitor and verifiers have very different architectures for example if the verifier is like a mini version of the generator versus if they're very different architectures but it doesn't matter like which one which one is all empirically okay so the question is like if the generator and verify it from the same family or not that's a good research question I think in general it's interesting like models like their own generations and their own way of interpreting results much better than using a different family of models we are going to talk about the paper that talks about how certain models can benefit from like a different class of verifiers over themselves but that is specifically I am aware of a study that has looked into that specific question of saying kind of size and family versus different size different family how does how that changes the paper.

I believe so you need test time when they're using the very fire like a baseline very fire versus themselves I'm actually not sure if they do that when they're doing process versus alarm because again that's really hard to do like but the very fire if you have if you're controlling for a fixed compute how do you allocate that so what's the challenge is here is that usually like the OR amount here should come from the same data for us to do like a good like apples to apples to apples because if you do train up here and someone else train an OR and fix things a bit harder the training data also matters a lot any other questions yes slide yeah I was just I don't know it was that basically for this one I was just curious as to why I'm using RL on the stuff with evaluating performances with reading building I was just curious like whether or not you know raising the temperature like the difference or that's the only way they could have seen like the smaller stuff so the the reason so the questions why did they start really calling why they didn't do like temperature changes and the test time scaling again right I think that's what this slide is so they they then in this case the verification is done against 256 outputs so the previous one is no verification right the very very fire is just using the RL training here is a test time as well yeah I think it's something that they didn't do and it would be interesting to see what if they repeat the process again generate another set of labels and train a very fire and then but starting from this RL PPO model yeah okay.

Let's get to the last paper shrinking the generation verification gap with weak verifiers so this is the paper a paper that from from Stanford that came out a couple months ago at this point so there's a new reps 2025 paper so the motivation again is the same we have we still have a generation verification gap and but the approach here was a bit different here we were not training a new very fire we were thinking can we reduce the generation verification gap by using inference compute and inference scaling and specifically by using an ensemble of verifiers because it's when we say weak we just assume that no very fires perfect so that's the weakness we didn't purposefully choose like bad very fires these are the best very fires that are out there but we are using an ensemble of them to create a single much more capable way fire.

So the property of weak verifiers is that they do have they do correlate with the true label of the problem but they have of the solution but they have imperfections and there are two types of these verifiers we learn about PRMs and ORMs but another one that we briefly discussed in the in in a few minutes ago is that we can use LLM's themselves as a judge we can show it an answer and ask it whether do you think the is correct or not the prompt could be literally that or we can have the model use tools and use rubrics and all that to generate a score for a model so that's a whole category of very fires that we can use on top of just PRMs and ORMs.

So here this first graph just shows how ensembling and using a number of verifiers that are they could be trained by different labs on different data sets we're just like and there are all these language models that take a query as input and give a score at the output side here we are comparing we are seeing these four data sets so in this case we showing the performance of top one versus top five versus top 10 verifiers based on like how good they are in some ranking system overall this seems like like just ensembling them with each other and using them at test time does improve things but not in a necessarily in a monotonically monotonic way but the thing that always helped was that if we train a model to a way for them to to add them together so I sign weights for different members of the ensemble if we have labels from them here we have we have two methods naive ways or logistic regression these are very simple matters basically you add a simple single weight for very far to score them together to have and if you have a training set set for for your with labels you can basically come up with these weights and and now these results are on the test set corresponding to those tasks so you basically say for math 500 I have a set of labels I use that to average my verifiers find the weights and then I use this averages on a new on test data so that does give it seems like it does give a lot a lot of improvement just this simple task now you can do other things.

So the way we were works is by this core weight and select approach so in the first step we are going to score outputs of the verifiers and normalize them so they're all on the same scale and and and filter out the low quality verifiers so in this set up even we are assuming we have access to a very limited labels that we can say okay these very far they're just so bad against our labels we just remove them from our ensemble we have noticed that this is a very important step like your very far should be above a certain quality to be even be let in the pool of verifiers so then you have your verifiers you have your LM judges you have your reward models and then you get these cores from them but there's zero or one down to the log prob of correctness per per verifier and then what we were did was to use week so week supervision to estimate kind of the very fire accuracy with very small data labeled data and then and then use those ways to combine the verifiers output and assign a score.

I go briefly through like how this week to strong supervision works some of you if you have taken deep learning courses or even see us two to nine as last time we talked about it about week to strong supervision there's a whole body of work here be there was specifically kind of motivated by the work by Snorkel by Alex Ratnell at all another work from Stanford so that so here is how we have these weak verifiers like weak signal from our verifiers and combine them together together stronger signal the setup is the following we have n queries for each query we create K solutions and then let's assume we have m verifiers so we have a total of n times K times m labels and our goal here is to create to get this probability of query i sample j equal to one given all the labels that we get from all the verifiers so how do we estimate this probability correctly the assumption in be where is that each verifier captures independent aspect of the correctness so this is a key assumption that we made to some extent the reason we get so think about it this way if you have a pool of verifiers if all of them agree on the score that they give to any sample then we are not learning anything new from them right if all of them are like hundred percent or the same score the signal here is on kind of similarity and this similarity across this whole set of verifiers with each other so under this assumption we can write this probability for the response being correct given the labels that we have and then we use two sets of equations again this the first equation is we can write it this way because we assume every two verifier i and j are independent from each other the second equation is just the base kind of the the the way probability works there's no assumption in writing this and given these two we can kind of define we can write an optimization and then come up with the weights that we can assign to each of the the verifiers.

And here we are comparing the results with a naive ensemble approach and it turns out that it does help in some cases that the gains are more significant and g p q a diamond and matt and m l m l u pro and each of these problems that are relatively hard problems for example the g p q a our baseline is already low but the verifier this way of weak to a strong aggregation of responses is helping getting a large boost over naive ensembling of them now there are different ways that we can scale the verification compute we can sample more generations so instead of ten samples we can do a hundred or a thousand we can use larger models for generation and verification we can increase the the number of verifiers in the pool and and in general they're different ways to assign flops or inference scale inference compute for for solving a problem.

Here we are comparing some of these methods with each other so the dashed kind of dark red line is the passive k oracle meaning that if we had like an oracle selector that would give us the correct answer we don't have that and that's why we're training this verifier there's the the purple one is we were supervised and that's assumed that we have a large body of label data and we can use them to learn these weights the blue one is the unsupervised in this case we're using 1% of the training labels for each data set the orange one is the naive ensemble meaning we just average them average the the the verifiers without any special treatment we still are filtered and keeping the good verifiers in the loop and then all of these are significantly better than methods such as majority voting or multi-agent verification a multi-agent verification just or may be is it's just based on prompting LLM's and asking them to score a given response based on different kind of aspects that is produced so kind of a rubric base which is doesn't do that that much better or actually worse in these two cases that majority voting but something to notice here is the drastic gains that the model can have going from something like 40 less like slightly over 40% to over 70% on these hard problems and in this case matching a model like O3 mini.

So the thing that is interesting here is that the we can use this big to super big to strong supervision to reduce the gap between model classes so what we are saying here is that if we take a generator model of Lama 3.18 being struck and the verifier model which is like the the pool of verifiers that are 8 B and below we get an average of 70% on these data set and this is almost comparable to the accuracy that we get with majority voting but when our models and the models are at 70 B so we are kind of like making the model making this 8 B model the same roughly the same as the 7 TB class by just doing this inference scaling and using the verifier and in this case unlike the what we saw in most of the previous lecture we are talking about the end results we're not talking about the coverage we're talking about the solution accuracy of the entire system and here again in this case for when we do the same apply the same generation and verification to 70 B class of models we are getting an average accuracy of 86.2% on these data sets and this is very comparable to this to O3 mini which which has a much which is like a proprietary model and in general is a higher like a different class of models so this shows how much verification or this work and verification can help with the results.

Another work that was done on the increasing the usefulness of something like Viva is that a challenge with ensembling verifiers that you have a ton of like models that now you need to run for each solution to get a score and then to average them right to that increases the cost especially if you instead of one sample per per query you have a hundred samples and then you want to create all these scores so instead what the proposal here is that what if we do train Viva once and then distill it into a much smaller model and that's how instead of running each of these LLM judges or reward models per sample we just take this distilled LLM which in this case we show that it could be really really tiny like something like 400 million parameters as opposed to the original model which was in the 70 B range and it turns out that with this distilled model we can kind of capture 97% of accuracy of this large pool of a lot verifiers but be significantly in this case 99 plus percent use less compute at test time and these and these distilled version and the original version they're all open sourced and the checkpoints are available if any of you are interested in working with them in your agent take or test time scaling projects.

And here is another graph here is the distilled one which is the light blue one and original Viva is a dark full one and we are comparing again the efficiency it's a success rate over the inference compute the total inference compute that we are spending on this data set and and the distilled one obviously is like significantly more efficient but even the original Viva because it reaches higher levels of accuracy at those levels it becomes more flop efficient than models like naive ensemble and majority voting and let's do a quick recap so here we today we covered like four papers that also show the trajectory of how this work and verify cow research on verification has progress over the last like four years we talked about how verification can improve training both during can improve the outcome of the quality of results both during training and inference passes reward seems to be very useful and overall very effective and more so than the outcome rewards but perhaps we need to combine them to get the best results both PRMs and ORMs and we also in match shepherd learn that scaling you need a lot of data and the more data you in the very fire training it becomes better and you can use it during your RL fine tuning to increase the quality of your generator as well and the Viva work well took a whole different approach to verification was and that was just doing this week's supervised optimization on an ensemble of verifiers and in a way we are using test time scaling but by bringing more verifiers rather than sampling a single verifier more to get the results and we can turns out we can distill that and capture a whole lot of the quality from a much smaller model and with that I can conclude the class any questions yes.

The model do you mean other areas of coding would be a very interesting area for reasoning so these like there were like some coding problems in the city like in some of the benchmarks here so it's not not just man but coding is also a very interesting kind of area for verification we will we will talk about code monkeys this is a paper that instead of directly training a very far you can make the model created test time system to make the model generate unit tests and those unit tests become your very fire so that's an entire different approach but these kind of verification should also be useful for any kind of reasoning task yes.
