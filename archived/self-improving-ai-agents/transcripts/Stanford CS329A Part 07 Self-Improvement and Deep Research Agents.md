---
title: "Stanford CS329A Self-Improving AI Agents | Part 7 | Self-Improvement and Deep Research Agents"
source: "https://www.youtube.com/watch?v=Uni9dqyuuDM"
author:
  - "[[Stanford Online]]"
published: 2026-08-03
created: 2026-08-21
description: "Want to dive deeper? This curriculum is covered in the following online courses: - Agentic AI professional education program: https://learn.stanford.edu/agentic-ai-2026.html - XCS329 graduate course: "
tags:
  - "clippings"
  - "cs329a"
  - "agents"
---

So today's focus will be very much on improving the models using Search. And there can be two kinds of search that we will see: one in code models where we are sampling a lot and then we are searching based on that, and then deep research style search, which you will see in homework three. So a lot of what we will cover in alpha code and alpha code two, those kind of search patterns you will use also in homework. Your homework two is focused on human eval. So this will give you an overview of what to expect in simpler problems and then even more complex problems. And then the agent research, this will be closer to what we will give you in homework three. I think the broad thing that I want to say with these models is that we kind of know that the solutions lie in the search space of the models, but how do you curate the answer out of the search space of what the model outputs is roughly what we are covering today.

So let's start with alpha code. So the problem we want to solve here is a more complex version of the simple coding agent problem. So if you use a coding agent, what you often get is some form of an auto complete where you have a line and then the coding agent will complete the line for you. So this is closer to a competitive programming problem. We are given a programming problem, like competitive programming problem, you are giving a problem description and then you are given some input and outputs. So you have some sort of a contract in that programming problem, which says that if you give this set of inputs, you should get these set of outputs. And what you want to really solve for is how do we solve these competitive programming problems. So the kind of things that we already are able to do is code completions, but when we try to do blocks of code or solve real problems end to end, that starts to be a much harder problem as you'll see in the ways that this problem gets solved.

So just to give you a preview of how alpha code came about and what it achieved, and this is an older paper, but it's worth remembering that at that point in time when alpha was released, which is almost like four years ago, it ranked top 54% among the contest participants in 10 contests. And this was the first time it was shown that AI can generalize beyond just narrow tasks and solve problems end to end. And this was a much harder problem than what you could do in just human eval, where you basically are given a very small problem, like complete dysfunction or you're given instructions to exactly solve what you want to solve and you don't have to reason about understanding the problem and then figuring out what it would take to solve the problem. So this was definitely longer problem descriptions, longer solution lengths, and you were having to understand from the doc string, what is the right solution approach even before going and solving that? So it's not just like taking the problem description from the programmer and then just like coding it up. And I think this is well understood that when models are generating suggestions in one single line, it's much easier for us as software developers to interact with that and you get much higher success rates, for example, in co-pilot and equivalent functions, but when you have to solve problems end to end, that ends up being a much harder problem.

So if you look at, and I'll go through each of these block diagrams one by one. So this is pre having a large model so they actually were pre-training their own models. The pre-trained models in the next report will get replaced. So what you see here is that you have a stage where you are basically coming up with a model from which you are going to sample. Now this model, we will later replace with a large language model. So here they're also doing pre-training and fine tuning. And the data that fed, that data that really helped this model train was GitHub data and then code contests. What code contests is basically a large chunk of problems and solutions that come from competitive programming. So once they trained the model here, they're basically going to do a large, they're going to sample a lot of solutions. So if we have seen repeated sampling before, so this is repeated sampling but at a massive scale. So you will end up with a lot of solutions and then you're going to play some tricks to figure out which set of solutions should we even run tests on. So far, we've just been closing the loop where we take whatever set of solutions, the model outputs and then running tests on them. In this particular case, you are actually going to select. So there's a selection stage before you're going to complete and execute and evaluate.

So just to walk through things step by step. So the first step is pre-training. You're going to pre-train a model. In this particular case, they're going to use mass language model loss. You can also have a decoder only model which we'll see shortly in the model is trained on GitHub code about 700 gigabytes of that trained with next token prediction. What's interesting is that when they fine tune it, they are using some tricks. So they're using regularization so that they can assign higher probability to more meaningful patterns. This is interesting in code and then they're doing something called as value conditioning and prediction. So the thing that I want to highlight in the slide is that in the regularization technique when they're doing fine tuning, they have assigned higher weight to higher likelihood tokens so that there are certain tokens that will have higher likelihood and then they are putting lower weight to lower likelihood tokens. So this is called as gold. So it will help improve the precision. So in the next token prediction loss, they are basically adding a weighting mechanism to take into account the likelihood of the tokens also, which helps in certain ways and helps to assign higher probabilities to more meaningful patterns. So this is done in the fine tuning phase.

So once you get this model that is able to, and this was an encoder decoder model that they were initially using and then they also tried with decoder only models in alpha code two. Once they get this model, they are going to try large scale sampling. They generate one million. One million is a large number. They generate one million diverse sample programs per question. So if you have a question, a competitive programming per question, they're generating one million solutions, half in Python, half in C++. So machines can do that. And they randomized the problem tags and the ratings in the prompt and they use a high sampling temperature. So it's like the solutions are going to be diverse. And once they generate these set of solutions, what they're doing is they are doing some amount of filtering and clustering. So they filter the samples to only those that passed tests given in the problem statement, and then they are clustering them so that they're syntactically different, but semantically equivalent. So they differentiate between that. So they are basically using a clustering mechanism where they use a separate test input generation model, train it to predict test inputs given problem descriptions, and then create these new test inputs for unseen problems. So they're basically trying to figure out different tricks to cluster this test inputs, the semantic equivalence being one way to do that.

Now the reason this clustering is important is it is effectively providing a way for us to select which set of solutions are diverse enough for us to then evaluate to close the loop. So now what you can do is you can submit only a subset of solutions to the code forces platform, which is the actual competition platform. So, and that will check the program correctness. It will benchmark against other performers, best performers on this task, like human competitors, but the challenge here is that you cannot submit all the solutions. So 1 million solutions cannot be submitted to this platform. So they have done a curation phase, a filtering and clustering phase to get to this stage. And then there's also evaluation on code contests. Remember that code contest was used to train the model. There's a test set that they hold out, which will basically give them a signal. So this is the dataset that is created by authors, but they can also measure signal on this particular dataset to figure out if their solution is supposed to do well or not.

So when they first, their evaluation on code forces platform, they ran, they took their model, ran it live, generated samples, filtered with example test, clustered to get some submissions and then submitted to the platform. And they ran this with, they submitted in 10 competitions, which each had 5,000 participants. And what they found was that their solutions with this alpha code actually had an average ranking of 54.3, assuming 10 submissions per problem. So not 1 million. And it was competitive with 28% of the competitors in the last six months. So this table is showing you that if you looked at a certain contest ID, how was the alpha code model doing? What was this percentage ranking, the best ranking and the estimated ranking and the worst ranking relative to other programmers? This is more of a ranking based as to like once you submit a solution, you're ranked based on the efficiency of your solution and the correctness of your solution. Before I move further, questions?

- This is your robot for previous page. It seems like the, this is the number, you look at the performance or the product even data size. It seems that - This is different contest. - Yeah, it seems a degree but I think the final one seems to switch. The worst one is like 53, 50,000, but if you look at some others, like once you submit, once you submit, like when you send a course, it's like reading books, like I think it's a most, I'm just curious why, like, I don't have a big variation of a couple people in Congress. - I'll ask you the question back. What do you think? I mean, this is your pipeline, right? So different contests will have different problem inputs.

- So the large scale, I mean, so what's your name again? - What I was asking is why do we get variants across different contest IDs and it does well in one contest and does not do well in another. So at the end of the day, you are depending on how close are these, say, contest problems, how in distribution they are. So that's one possible hypothesis. The other possible hypothesis is that our ability to, so in the large scale sampling, that would control, like, is one of the solutions correct but then our ability to select the right set of candidate solutions might also be affected in different contest IDs, right? So the selection stage can also be a bottleneck here. Like there can be solutions that are almost correct but not completely correct. Yes.

- So I think you basically went, so they do have some experiments that they did but I think it's sort of order of magnitude. So with this particular model size and there is some results I'll show you, with this particular model size, they had to go for a much larger number of samples. I think if you remember from earlier slides on test time compute, when you have smaller models, you do have to go for much larger number of samples to get the same passive key. - So let's look at the results and then come back to that comment. But I think on the question around why is it different in different contests? I mean, the inputs are different and then the pipeline will, the selection stage can be a bottleneck. So both of those influence where it does well and where it doesn't do well.

- So there's two things worth noticing. So when we compute solutions with repetitive sampling in test time compute and this is something that you're definitely using in homework two, you have this notion of pass at K. So that's the percentage of problems. So if you get the model to generate K samples and you submit all of them for evaluation on the hidden test, how many problems will actually solve the problem description that was given. It measures how good is the search aspect of the sampling process. So it's basically coverage in the lectures that we covered before. What this particular benchmark measures is 10 at K, 10 being the number of submissions that you're going to go submit for evaluation. So even if you get the model to output K samples, in this particular case, 1 million, you don't have the ability to measure correctness on all the samples for the final set of solutions. So you do have to do some selection or scoring to decide which set of solutions I'm going to submit. So it measures also the filtering process of how well the model will behave when you have a very large number of samples. So you're not just generating samples, but you have to do some search yourself before you can submit the solutions for evaluation to hidden tests. Now, why does this matter?

validation set and that's tested and they train different model sizes. So this is 91 B model. So that's a larger model and then 41 B with clustering. And what they're presenting is validation set results for 10 at 1 K. So this is the question you were asking me. They're basically going to submit 10 solutions out of 1 K generated outputs and then keep increasing that to 10 K, 100 K and 1 million and then they are going to do a similar set for test set as well. And what you will notice is that typically the larger model size consistently will do better than the smaller model size. I think that is something we have talked about before. But the other thing to notice is that as you go for larger number of samples, which is something we have seen as well, you are able to do better. So like going from 10 at 1 K to 10 at 1 million, you're clearly doing better. set side and the clustering. So typically we had covered this slide with pass at number of samples. In this particular case, even clustering is consistently. So 10 at 1 K will definitely also improve similar to pass at 1 K. And then if you look at the last row here, which is 41 B clustering, you see that that's consistently better. So like 9 B versus 41 B versus 41 B plus clustering, the 41 B plus clustering is definitely going to still be better than the one before because it definitely provides an improvement. And the reason for doing that is because if you were to measure the set of solutions that the model has output and you wanted to measure their diversity, the clustering provides you a way to submit only the diverse set of solutions if you can only submit 10 problems. And since competitive coding problems are quite difficult, you don't get much benefit out of submitting the same set of solutions to the evaluation process. So if you can choose which set of problems to submit, that helps.

And if we were to measure just 10 at K and pass at K, if you had, so pass at K would be unlimited attempts and 10 at K would be 10 attempts on evaluating for a problem. On the X axis, you have the sampling and on the Y axis, you have this accuracy in terms of how many attempts you get to solve the problem. The solve rate definitely scales log linearly with more samples. So this is very much in line with what we had seen for pass at K and repetitive sampling. So even after the selection stage, we definitely see the same log linear trend with more samples. And better models have better slopes. So in this particular case, you see that 41 B, which is the blue curve has better sol, sorry, the purple curve. 41 B has better slope compared to the 300 million parameter model at the bottom. And same applies even for pass at K. And then the final thing is that when only 10 samples are submitted, then you do get bottleneck in certain ways. So you have to, you can get higher accuracy. So if you compare the Y axis, 10 at K versus pass at K, if you have unlimited attempts for a problem, which is synthetic scenario, you are almost getting to something above 40%, and here you are only getting to 30%. So there is definitely some bottlenecking happening selection stage. So you are bottlenecked by how you filter and cluster in some ways, when you have to choose which set of solutions will pass the test. Yes.

- Well, the point of alpha code two, making the model better is a slightly easier access than scaling the number of samples that you're going after. So on alpha code two, the model performance improves helps you reduce the number of samples to get the same performance, or at 1 million, you'll get a much better performance. So this is an older model, but it's a weaker model, but the trends generally hold. And in some ways, there's a, I mean, the larger models have done better, generally speaking, but there's a model capability gap. So if the base models are not strong enough, they're going to hit limits at certain point.

- If the costs are not prohibitive. Yes.

- That's a good comment. So one of the challenges with solving in our ways is that you kind of assume that if we sample more, the diversity continues to increase. One of the challenges that we will see in alpha code two and I'll emphasize again is that you do have to ensure that as you sample more and more, getting more diverse solutions. And part of the process of clustering is that it's effectively figuring out which are the diverse set of solutions. So if you sample 10x more than what was sampled here, but you don't end up with more diverse solutions, then you're actually not going to improve. - The reason why we have a local linear relationship here, why is it not any other modeling of wider sets? - So this was covered earlier. I think the pass at K versus unlimited attempts, this is theoretically derivable. And I think the main thing that I'm emphasizing is that even after the selection phase that log linear trend holds. The pass at K versus sample budget, this was covered and I don't know the lecture number, but Azalia covered this and that paper actually theoretically derives it. This is the large language monkeys paper or the one right after that. Yeah, but if you look at the derivation, we can work through that offline.

Okay, so if we look at takeaways, I think the fun aspect here is that we can get high coverage by sampling. And then so this large scale sampling, this filtering and the clustering approach, you can get higher coverage. And they were able to actually also make sure that the model is not just copying existing solutions. So they did some search over the training data and actually saw that there was the generated code with had some novelty. So this was actually generalization out of distribution. The challenge with these particular set of approach that they noticed that's worth highlighting is that they were training the model on loss. And loss is often a poor proxy for solve rates. So because there are many solutions that could have solved the problem. So they did see that this model did not do well on say dynamic programming or constructive algorithms in some ways. So it was not the strong and certain domains. And then the other aspect was that it was requiring a large scale set of sampling. So that was not ideal in terms of like how practical this is to be used everywhere. If there was a time bound, then you can imagine. So if you basically was just examining accuracy, then you want to make unlimited attempts. But if you want to do like time limited bound in terms of efficiency, then you do want to like rank acceptable solutions as they get generated. So that becomes really important. And as I already mentioned that more difficult domains required more experiments. So this was not like the, this solution approach actually wasn't working well on difficult problems. And in fact, there it's both a matter of like, you might need more steps to solve the problems. So this particular approach in one shot did not do a reasonably strong job. So multi-step solution approaches would do better here.

So this was alpha code summary. Now let's see what can we do better? So this was like 2022. If you look at the next set of paper of which is alpha code two, they improved upon the substantially and that was really helpful. And we can also learn what were the differences and that really helps us understand what pushes the front here even further. So alpha code two started with the hypothesis of what if you don't pre-train your model, you use an existing LLM. So in this particular case, this work was at Google. So they decided to use Gemini Pro instead of like using training their own model. And then they want to customize it to get better performance on this particular problems that on competitive programming. So the first set of changes that were made were that instead of pre-training, you basically are just going to fine tune Gemini Pro. So it's not just prompting, they are fine tuning the model. And then in terms of sampling and evaluation for the large scale sampling, they want to ensure diversity. So for diversity, they actually had multiple alpha code two models that allow for massive sampling. So when they did and go and do fine tuning, they actually had multiple variants of this model so that they get diversity in the output sampling. And the third thing they did was that to select the subset of candidates, they had a scoring model. So you can see that as a reward model, which was used for obtaining the best candidates. So it's not just based on clustering and filtering, which is a heuristic for saying, okay, this would be semantically equivalent, but there's diversity in syntax. They are actually now going for a scoring function, which can like learn that function and the approximation of what should be given high score and what should be given low score.

So the new ideas here as I highlighted was A, that they are going to fine tune a Gemini Pro model to score correctness before submitting. So that's a reward model. And they also had a family of models based on different hyperparameters, so different difficulty levels, different tags. So they fine tune several models with varied hyperparameters and then that maximizes the diversity of the number of samples they generate. And they also improved their data sets. So the data set that they fine tune on is a VT version of code contest, which is actually open source. And then they had another high quality data set for scoring purposes. So going back to this diagram, what changes next? So let's take a more detailed look. So here, if you look at the input model on the fine tuning state, you have this new data set called code contest V2, which is going to be used to generate. Now, you're going to do some tagging. So there's some amount of just like, take your code contest V2 data set and put a bisected or rather segmented into different data sets to fine tune different versions of models that are going to generate samples. So that's one set of like intermediate alpha code two models that they will generate. And then they had another high quality data set that they're going to use to make this even better. And this same set of data will also be used to generate a scoring model from the same model. And based on that, they're going to, so this is basically a notion of like the data set is segmented and used to tune multiple models to get more diverse samples. What's different about the data set is that the problems and solutions are higher quality and vetted and scored better. And the scoring model is able to estimate the correctness of the code sample between zero and one based on this higher quality data set. So they manually curated this data set, got more human annotators in place so that they could train the model to estimate correctness better. And then the other aspect is that they had this family of models instead of having just one model.

Now on the sampling and evaluation side, they split the sampling across models. They in fact went to only C++ earlier. It was C++ and Python half samples. So they split the sampling across models, but only C++. And when they generate the samples, they're randomizing the temperature and the metadata so that they can get diverse outputs. So this is the massive sampling pipeline. For each output, they're going to execute on test input and they're filtering out anything that's incorrect or doesn't compile. So they remove 95% of the samples. At this point, they're left with about 50 K samples. And then after that process, they're going to aggregate and keep the top 10 largest clusters. There's re-ranking on top of that based on the scoring model to say like, how likely is it to be correct? So they score each code sample and pick the best candidate per cluster. And once they have that, that's when they're going to submit and say that did it win or did it not win. The eval is going to be same as alpha code.

So if we are to compare results now, this is comparing alpha code two versus alpha code. So alpha code, so on the x axis, you see the sampling budget. Alpha code we were doing one million samples. Here we are going to vary the sampling budget per problem. And on the y axis, you have to solve rate. As you can see, that once you get to 100 samples, you basically, so alpha code was using one million samples for alpha code two. Once you get to 100 samples, you are achieving the same solve rate as alpha code. And if you want to go beyond that, you can use more samples. So the solve rate is still improving with the increase in sampling budget. And if you were to basically say, what is the maximum performance achieved? Alpha code two with one million samples was solved, was solving 43%. It was getting 43% solve rate while alpha code was only getting 25%. So in this particular case, even though they're using the same number of samples, a better base model, better diverse solutions, and then better scoring is giving them. So this whole system is giving them performance gains, which is almost 2x relative to where the previous system was. Questions?

Yes? - 95% of sampling wasted on not compiling, poor answer seems like a very high cost. Were there any other studies to see if there could be better methods of sampling one of the vital answers, maybe by the prompting, or I mean, they'd mention randomized temperature for work, were there any other approaches? So this particular case, this paper did not, but if you are to refer to some of the work that we have discussed before, what would you need to do to improve that? - This is a masterpiece award, right? - Yes. I would try the approaches we took in homework one. - Like better prompting or better maybe using instead of wasting on the compute time, one million sampling, maybe I would try to.

- So I think what you're saying, so just to repeat the answer, in homework one, you had done some form of self-refinement. So this is essentially parallel search in some ways. You've generated a lot of solutions and then you are clustering on top of that. If you do a refinement process where you have generated some solutions and then you're saying, okay, if I can get any feedback, can I improve the solutions better? That's one approach that would cut down the number of samples but increase the time that you, if you can collect any feedback. The question I would ask is that how would you collect feedback? What would be feedback in this particular case? That's worth thinking through. I think the other comment would be that if you can get the model to do slightly better with RL, so if it can get better at solving things with the RL loop, in between when we cover train time scaling, then that can help cut down how much we need to put in test time. So both those approaches can reduce the cost on the sampling side. Let's try that one. - So when it comes to a perfect application, so when we are using everyday thinking models, we are still talking about a couple of dozens of samples, right? I mean, when it comes to time scale. And then we hear about this open AI type decay per month researcher. Those are the multi-thousand, multi-hundreds of cases of samples applied in production.

- So I think the nature which have the system is more of how you would build the system that is generating those dozen of samples, right? I think we have discussed this before for us to think about these systems. I think the big question you're asking is, how would you build the system to begin with that can reason well, that can close the loop? So I think there is a line of research which has tried to answer that question. And what this is covering is that if you started with an LLM and you wanted to solve these complex problems, how would you build a system that can solve that problem? And then if you can distill all of that knowledge into a single model, then that becomes a large reasoning model. But this is a more of a multi-agent system in almost some ways where you're basically having one model produce outputs and then another model score it. And then here a family of models is producing outputs and then there is another family of models that's scoring it. So that gives you more tricks up your sleeve if you had to like re-architect the system from scratch as opposed to depending on what the current generation of models cannot do. And the paradigm I believe because at the end of the day, when we are saying in test time compute, there should be a solution in the space of what model outputs generated. The paradigm is effectively that you should be able to search that solution. So what this is roughly answering is how would you search for a solution in the output space of what models output? So why is the screwing model? Or does it use the procedure? - Say that again. - Why is the screwing model actually that means so? Why is it holding? Why is the screwing model only desirable? - So basically we can't just advise the screwing model not using code contest V2. I mean, you can train your, so the code contest V2, there would be contamination, right? The screwing model should not see exactly the same data, but it does need to see some data in the distribution, right? It's basically a matter of you're training the screwing model not necessarily on the same problems, but you want to, if you're designing that system from scratch, how would you train a reward model? What you want to show it is that for this kind of problems, if you had these two solutions, which solution should you prefer? That's what you're teaching the screwing model, or what ranking should you apply to that?

- I mean, you could make those data sets and then done the same thing. Yes, but that is a doable exercise. It's just multi-stage. It's not the same in the sense that when you do any kind of fine-tuning, if you're changing the quality, then the model, and if you're not makes sense, using the previous version, and the model is forgetting some of the previous information when it's tuning in the last stage, okay?

So if you were to look at the aggregate results of alpha code two in terms of the normalized score versus the, how it would compare to human contestants there, and the way you would do that is you would basically compare the percentile of contestants who score at a certain level. What alpha code two was achieving is 85th percentile between the expert and the candidate, or the master candidate solutions, while the human, and it was, while alpha code was outperforming 46%, alpha code two, if you took a top two performance, if you took two top two solutions, then it was actually outperforming the five percent, but overall it was basically scoring closer to 85th percentile across human contestants, so that was a big deal. It was like, okay, in this particular code contest problems, in this competitive programming, it's doing very well in some way. So that was a big accomplishment in one single year, just with an improved system. And I think for us, what's worth learning here is that we are increasing the performance with less number of samples, so we didn't have to necessarily scale the number of samples, but we had like better foundation model, and then the scoring model that is selecting the best candidate is also helping the solution. The experimentation is still quite costly, and then as one of our classmates pointed out, this is very specific to code, and then a lot of the computers wasted in generating samples that might have bad syntax, and so on, that you have to go and filter out.

So one set of questions that I have for the class at this point in time, is that how would you change these methods based on task complexity, for example? So actually I will pose that question, and then the other question I will pose is, how can be embed reasoning directly into these models? Take a minute to talk to folks next to you, or somewhere in the vicinity, and then let's come back and discuss those two questions.

Let's tackle the second question. So how can we change these methods based on task complexity? So if I have easy problems versus hard problems? Any takers for that? Okay. So you're saying that if you have a simpler problem, then you will sample more from. Okay, so in our culture, basically, you will use the model that's trained on perhaps simple problems. Is that the suggestion? Okay. Other ideas? The solver did improve with model size. That's... So this is something that was probably seen in the rapid sampling and the task time computer as well as to like, if you have simpler problems, then it's likely that you can get coverage with fewer number of samples. So you don't have to go to 1 million samples every single time. It's almost like saying the model does more likely to generate the solution in the search space of what it outputs. So it should be easier to sample, or its initial set of solutions with some iteration, with some iterative refinement, should be easier to correct as opposed to like having to generate a lot of different solutions. One of the gains that you get if you generate a lot of parallel solutions is that you're trying to get the model to provide diversity in the approaches, and you're not trying to get it to like fix what it already output.

What about the question for, how can we embed reasoning directly into the model? Okay, let's try out. Okay, so if we don't have enough common data that we use in LLM to kind of pass through as a code, like to see the first section that is a lot more than the second section. Okay, so what he's roughly saying is that you add hints in your training dataset around the solution of like what kind of algorithm is being used to solve the problem. What's a more generic version of that? What have you already covered in that line of work? What do you think before you write the answer? What if that thinking could be captured? If you added those chain of thought as part of your training set, or if you remember the star approach where you're getting the model to generate the solution and then like, give the hint as the answer and then ask it to come up with their reasoning, then that can also be part of like how the model embeds it, right? Any other ideas? I think the other interesting thing that was mentioned which is probably worth highlighting is that if you have a more complex problem than you might want to decompose the problem into subparts in add hints for subparts as opposed to trying to solve the problem in one go, because these are hard problems. Yes?

So I think all of these are hinting. We do need to close the loop at some point in a multi step fashion and almost want like a tree search style of solution where you basically want the die. I basically sample for say the first step and is that roughly correct and you want to sample for the second step and then you want to be able to backtrack. So it's roughly heading in that direction, but it's kind of early work that gives you hints. So it is basically gotten your thinking and then perhaps that's your project, right? But your question is very relevant as to like what happens if your answer is correct in the first step, but then the next step is not correct. And the task decomposition is often relevant in the most problems have a certain way of solving. So if there is common abstracted patterns, then that's doable. But if you do expect patterns that are out of distribution, then maybe you do need human in the loop. That's like a scientist style of work that folks covered last lecture.

Okay, let's come to the last part. So we spent a lot of time on just code and how to select a set of code samples that you can let them pass and solve competitive coding problems. Now let's look at how would you do something like building a deep research agent. So search O1 is basically going to use large reasoning models as the base and then try to build deep research agents on top of that. And it's worth paying attention because you will use this in homework three. So the key idea here is that you want to use large reasoning models to retrieve based on a query that you want to give the model. So large reasoning models have impressive reasoning, but as you know that most of these models are trained on with some knowledge cut off dates. So they typically will not have the freshest knowledge. If something happened yesterday, the model will need to go look up that data. It's not going to be there in the model. The other challenge when you work with the reasoning models per se is that they will go and output a lot of tokens in long form reasoning. But when they have knowledge gaps, they will be using terms that are expressing uncertainty. So one example is that if you go use benchmark on GPQA data set, you will see the term perhaps in the reasoning chains a lot or you will see the term alternatively or weight or. So they're basically expressing that the knowledge gaps will show up as uncertainty in the way the model is thinking. So you want to bootstrap that in some way because otherwise those knowledge gaps will continue to propagate through the entire reasoning chain.

Now one simple solution based on learning from feedback or learning from tool calls is that why don't we just like take this query and get a relevant document. So you take this query, pass it to a search tool and get the relevant document and then put that document in the prompt and then out get the model to output based on that. Why is that not enough? So that would be more like retrieval augmented generation. So whatever is your question, you generate a query and you get a document and you put it in the prompt and then that's what you use to retrieve the answer. Now the challenge with that sort of simple approach is that you retrieve once at the beginning. So you don't have the ability to tweak things and it's possible that if your search problem is complex enough, then each reasoning step will need different piece of information. So if you have a problem which is asking just the weather, then a single search call is enough. But if you're asking to solve a complex problem which has multiple parts to solving it, then after it gets to a certain amount of reasoning, it needs to go look up. So for complex reasoning, it's extremely hard to get it right and same for multi-step reasoning. So typically retrieval augmented generation will show improvement over direct reasoning, but in multi-step reasoning, it definitely suffers.

So the solution that search over and proposes is that the model generates the queries on the go. So whenever it sees these knowledge gaps, it will trigger a query, a search query of what tool calls to generate and go search things over. And there will be multiple iterations in a single reasoning session of fetching these documents. And the second thing it will do differently, and I will show examples of that is that it will analyze the retrieved documents. So instead of just dumping what it retrieved as the document, it will analyze the retrieved document and extract relevant chunks of information from there. So it will only put these relevant information into the prompt so that it integrates well into the reasoning chain. So just to reiterate how this works, so this is an original question which is a chemistry question which is talking about it wants to get the carbon atoms count of product three, which is output of a bunch of different chemical reactions. So if you were to use a large reasoning model, will ask the model to start thinking. It looks at this term, which I don't understand and perhaps the model doesn't understand. So it will go ask a look up what does it mean. Now, one trick is that it doesn't go look up. So it makes a guess and comes up with whatever is in its knowledge base in the store weights and provides a final answer based on what it gets. But if there is a guess here and it got the answer wrong, then those things will cascade as errors into whatever is the final answer. So all the perhaps or all the uncertainty will basically cascade into the final answer. So your certainty for the final answer is low.

Now, one possibility is that you basically whenever you encounter unfamiliar knowledge, you can now do web searches. So that would be a simple scenario of like, okay, unfamiliar term, basically go and do a tool call for search, return the document that basically has that search tool for perhaps there's a Wikipedia page or there's a web page that has that information. that in the reasoning chain and now like do the put that in the prompt and then do the call again and then provide the final answer based on that. The only challenge with this is that as you basically increase your amount of content you're putting there, there might be not as much relevant content in these documents. Like if you went and fetched like 10 documents that were related to the information, there's too much information for the model to process. So it might actually not still give the correct answer. So in this particular case, was like 10 carbon atoms and product three, it was like 14 carbon atoms that still got it wrong.

What search O1 is proposing is that now you have whenever you get the unfamiliar knowledge, it will go and search for helpful information. So that's still a tool call to your search, but then it will reason within the documents. So it will try to look at these documents and see if this is helpful information or not extract the relevant content and then put it back in the next prompt like what is the relevant chunk of information to put and that will allow the model to continue coherent reasoning and provide the final answer here. So in this particular case, basically the ability to do the summarization of information or reason within the document really improves the quality of like how it gets to the final answer instead of like clustering the prompt with like 10 documents or 20 documents. So what's the difference between the three approaches? Yes.

The harder part is how does it know what to search for? It needs to know which keywords it does not know. That's the question I would ask. The answers, I think you basically need to break down what are the key. So there's different ways to tackle that question, right? If you're building a real application, you want to identify which part parts you don't want to rely on the knowledge base of the LM and you actually do want to do a tool call because that information is likely not what you want to trust the weights of the LM for. It's almost like you're finding the key entities in your question and fetching information on those.

So there were two things that we are covering here. So one is the agentic rag and one is search for one. So I'll cover both here. Let's first understand agentic rag because then we build on top of that in the search or process. In agentic rag, what you're doing is you're basically every time you are hitting entities that you care about, you're basically going to insert special tokens where you are going to trigger a search and then when you get that document, you're going to insert those results. model is generating some output tokens. When it's seeing certain amount of uncertainty in its output tokens, it's generating the search queries between the special tokens. And based on that, it goes and does the tool call and the retrieval tool will essentially return those documents, which will then be inserted into the reasoning chain. So this is happening in large reasoning models as part of the whole reasoning process of like it's also calling tools as part of the reasoning process. So that's one way to solve it. But the documents can be lengthy and they can contain noise. And oftentimes depending on how good the long context understanding and reasoning of the model is, they might actually like if you put 10 or 20 documents, it might actually not be able to do a good job at reasoning over them.

So the reason in documents module of search O1 will allow you to do analysis of each of the documents of like how relevant the content is. And then it considers the current query what was reasoning previously like this chunk of information that it extracted and appends that to the prompt to allow the model to continue reasoning based on that. So it's essentially a multi-step process within that retrieval tool function itself because it's basically getting that document information but then also doing an extraction process on top of that. It's almost like as humans, you don't want to just like go and curate all the possible references. You also want to take notes on those references so that you have some way of like doing a better job understanding and synthesizing that information. And this allows the model to do a better job at reasoning when we are adding external knowledge. And especially for complex queries, this becomes quite important. Yes. - So there's an assumption that there is a state or there's a memory buffer that's keeping that. It's not obvious in the way this is structured but there is basically some form of like notion of like this is the search query I'm trying to answer. This is and some buffer of like where is the previous reasoning and so on. Yeah. Yes.

- So is that like directly that's working the second thing. It's not like a, I mean, like because it's directly mixed method to keep the, it's a new method for false limits. It's true. - So you can just speak about it. - I mean, like if you just like modify the prompt into the agent, you grab a module, like you ask the model to do some summation and lose and then continue the reasoning that's that produces the same results of this. - So I think that's a great question and I would encourage you folks to try that out and I'll work three. I think the assumption there is that if you were to issue that prompt, you are assuming that the context length of the model and the reasoning over that context length is working really well. This is basically agent context engineering style paper. There's a recent paper called agent context engineering. So essentially the limitation here is coming from what can you put in context length that the model is going to do a really good job at. In ideal world, we will want that. That's where we want to shoot and hopefully by the end of the year, that's where we will be.

Okay. So let's look at the examples. So we were covering this example of like carbon atom count and product three based on a bunch of different chemical reactions. And when another reasoning, the challenge was that it was guessing the structure. So it definitely got wrong answer in agent derived because you were just retrieving the documents. They ended up being too lengthy. So it did disrupt the reasoning. In search of one process, it did search for this particular formula and then it refined it to a certain structure and then that integrated cleanly into reasoning. So essentially the notion of like extracting the right information from the documents really helps search over and get to the correct answer in this particular case.

Now, if you were to go beyond the example to results, what we're plotting is different domains, physics, chemistry, biology and overall. And you're plotting pass at one on y-axis and then you're plotting the number of documents that you get on x-axis. So typically as you increase the number of documents, for direct reasoning and for RAG, the accuracy does not go up. But in this particular case, as you increase the number of documents, you are actually able to do better because you can summarize what is relevant information or not in this particular case, assuming that fetching more documents gives you more relevant information related to the problem that you're solving. So this is basically another axis of like instead of doing iterative refinement, you can do parallel refinement because you can basically go fetch more information and then chunk it down to put it in the model context.

Now, if you were to compare the performance on GPQA data set. So if you just compared GPQA data set in physics, chemistry and biology, I was showing you a plot in the last slide. Human experts in physics, chemistry and biology on GPQA would score this much. If you'd look at the reasoning models and you build search over and on top of them, what you notice is that, for example, in physics and chemistry, search over and actually is quite competitive, sometimes even better than what human experts would do. These are hard problems by the way. So in biology, it's almost competitive to say that biology is solving the problem. One comment I want to make is that you want to focus on physics, physicists solving physics problems. So you want to focus on the diagonal here. Maybe not obvious, like if you change the domain then that's the number you're comparing to. So the search over and is doing pretty well in physics here and in biology here and in chemistry, not so much. sourcing the physicists. I think you're definitely getting to a point where the models are able to do well in certain domains more than the. But this is not to say that you're outperforming human experts. It's more to say that you're competitive with the human experts for this class of problems. Yeah. So this is a lot of headroom in chemistry. Yes.

Yeah. I'm just wondering whether it had a perform force in the entry as in how the entry part. How the problem is. How do you better preserve part of the problem? How do I hope that it's supposed to be something like that? It might be. I would like to actually go and try to evaluate a GPU and see if the chemistry segment in that hypothesis applies. It is possible. I mean, every subject where you go and prompt these models, the level of expertise kind of varies for different reasons. Right. It's also possible to train is not that strong. So it's out of distribution. And the other comment that I was making earlier, if you basically, as you increase the number of documents, this particular technique is able to leverage the fact that as you increase the number of documents, you can improve the performance just because you're not just making the context longer. You're actually providing relevant context from each of those documents.

The other big thing that I want to highlight is that in multi-hop question answering, this is particularly relevant because in multi-hop question answering, you do tend to hit some amount of saturation in terms of performance with standard drag. So and even with agent drag, you do tend to hit some amount of saturation. So in like Hot Pot QA in two-weeky music or Bamboo Google, for example, if you just did drag, for example, you would not hit the state of the art numbers. But with search oven, in a lot of these cases, you are hitting the highest numbers, the bolded numbers across that particular line are coming from a search oven as a process. So it is providing a lot of value, even like in the multi-hop question answering across multiple documents. Yes. - What are the decisions we call every true motion? - That's a great question. - Like the ritual itself is about it, not the model. - So I think an assumption here that you did draw at least for the way I am explaining, I mean, it's possible that you didn't retrieve the right documents. But if you look at the set of questions, I think the main claim that this paper is making is that if you find multiple relevant documents that are loosely correlated to what you're querying about, even then, like once you put all of those documents in the context length, then you're asking the model to do a lot in terms of or some reasoning over that many documents. So that's the claim that this particular paper is making.

So in terms of, like, if you were to look at the takeaways here, the gains are definitely coming from the fact that it's able to reduce the uncertainty. And the document quality or like what you basically refine knowledge, both of those improve. And in the multi-term scenario, once you can figure out what you don't know, it's able to, even after refining across documents, it can do this iterative loop of improving upon that. So it can construct better searches over multi-term scenarios. So in this particular case, the reasoning quality improves substantially because you're providing more relevant documents and then you're also figuring out where the uncertainty is over multiple steps. So effectively, you have the right documents you can correct yourself because when you put the documents in there, if you're still seeing uncertainty, then you have additional searches to go for. So there were some numbers that the paper gives of like the number of perhaps or weight or likely, basically anything that quantifies uncertainty in the reasoning chains goes down substantially in this particular approach. So they're basically analyzing the reasoning chains of the model.

So just to summarize what we've covered here is that when we are building deep research sort of agents on top of say the large reasoning models, that's a very effective way to like bridge the knowledge gaps that exist in large language models. But just doing simple retrieval augmented generation is often not enough. And even with just fetching the documents using tool calls is not enough. You need to be able to do that effectively over multiple turns and then identify the relevant information to refine which set of documents to keep. So the large reasoning models by themselves often don't do a good job in scaling well if you put a lot of documents in there. So this particular approach allows you to improve on that. And larger models often do better. And then, okay, there's another paper that I wanted to cover but like didn't end up covering. So the search R1 stuff effectively varies from search O1 in that it teaches the model how to go search in an automatic way. So what I wanted to say here was that search R1 versus search O1. Search O1 is closing the loop with a prompting based approach. Search R1 can actually teach the model to do this with an reinforcement learning based loop. We will not cover this paper today because we are basically at the end but it's worth going and taking a look at that paper.

Questions? Yes. The following is closing the definition of a language model. So then we generated an answer. We supposed to have the ability to put a probability of the answer. And I'm just wondering that then we have this, let's say 1,000 generations. Each generation has a kind of different probability or at least in some extent. And has there been some correlation between and the right answer and the probabilities? Or they are by the right definition, they are also being uncorrelated? And also same question to RAG. That's when we have a RAG-almented generation. I can imagine that some of the maybe it passes out in some of the definition. Is there any difference in the probability of the answer?

I think that's a great experiment to go through, right? So let me answer the first question. And the second question, you will have to go experiment yourself. So in the first question, typically-- so there's a lot of studies. But if you were to just take the output generation and compute the log probabilities of the output tokens and aggregate them in some meaningful way, like not just some, you'll have to do some averaging or some geometric product or something, what you will find is that the models tend to be over confident about what they-- over confident. So if you were to calibrate that to the right answer, often you find that the correctness is-- if it's 50% correct, it will still be overconfident. We're like 80% very confident that this is correct. And this kind of shows up in the ways in which if you try to correct the model outputs, it will sometimes be pretty strong and like, no, this is the right answer and will not change its mind. In some ways, the model outputs have exhibited not high calibration and are more on the overconfident side. And there have been research to try to get the model to estimate the confidence, say, in a second pass or in a-- and try to do RL or RLHF on top of that to get the model to calibrate and do things so that it doesn't actually emit answers that it has low confidence over. So there has definitely been efforts in that direction. Different models exhibit different behaviors. We don't know which lab did what. So we just get these notions of like, oh, the model is hallucinated that it knows the answer. That's been the observed pattern. But this is worth studying. The hallucinations and just the ability of the model to know what they know is an active area of research. And it's actually a great set of research projects if someone wants to pursue it.
