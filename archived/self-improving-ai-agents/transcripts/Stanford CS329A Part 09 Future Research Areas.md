---
title: "Stanford CS329A Self-Improving AI Agents | Part 9 | Future Research Areas"
source: "https://www.youtube.com/watch?v=AyO6wyu4DEg"
author:
  - "[[Stanford Online]]"
published: 2026-08-03
created: 2026-08-21
description: "Want to dive deeper? This curriculum is covered in the following online courses: - Agentic AI professional education program: https://learn.stanford.edu/agentic-ai-2026.html - XCS329 graduate course: "
tags:
  - "clippings"
  - "cs329a"
  - "agents"
---

Okay, so we will start with an overview of what we have covered in this quarter. So we started the class with just giving you an overview of LLMs and how over the past year test-time scaling and train-time scaling using self-improvement techniques—where the fundamental loop is driven by verifiers and feedback, running those verifiers and getting rewards, and then using reinforcement learning and even search algorithms to help the models to hill climb on math and coding, for example—have been a lot of the focus of the initial part of the class.

From there, we started talking about evolutionary strategies. So there was a whole class on open-endedness where we're not just driving self-improvement loops with rewards. We also allow the model to just go explore if the exploration space can be defined. And here we discussed AlphaEvolve and similar techniques. Then we moved to a paradigm where we built end-to-end workflows for agents using tool use. And as the models interact with environments and tools, you basically are able to drive the workflows end-to-end. And if you imagine that if you have tasks that need to be completed over multiple steps, then you do need to have perhaps the ability to look at knowledge bases, so that requires retrieval and memory. So basically, as we go towards real-world workflows, we need better planning and multi-step reasoning in these models. So we discussed some works around that and how to really look at evaluations in the next generation of models and what capabilities would drive that. And then we had several guest lectures on post-training and multi-step model agents and robotics and reasoning and so on. Azalea, is there anything you would like to add here?

Yeah, I think this sounds good. This is full coverage. I guess some of our guest speakers also talked about the symbolic techniques, which goes under the umbrella of tool use and how that could add to the synthetic data, which is another trend we are seeing.

Amazing. Thank you. So I think just to recap, and the class is named as Self-Improving Agents. So it's not just focusing on LLMs, it's focusing on the agent aspect of it. What that roughly, if you were to remind you of what we discussed in the first class, is that how is an agent a generalization of an LLM? It has a goal and it will go and interact with the environment, collect feedback, and use that feedback to correct its steps. So they're basically systems that can direct their own processes, use tools, and then accomplish a goal. And oftentimes in the current paradigm, the LLMs by themselves are not powerful enough to drive towards a full goal. They're getting there. So oftentimes we would handwrite these systems as workflows where you are orchestrating LLMs and tools. However, in certain scenarios like coding agents, you are seeing some of these agent workflows being driven by themselves. So it's an exciting time where we are seeing a lot of progress in this area.

And how we construct these workflows oftentimes means you're orchestrating LLMs, you're orchestrating verifiers because you need some form of reward. You might have LLMs as judges for verifiers, you might have tool calls, you might have search algorithms in there. So you might have parallel LLM calls that are going and doing this kind of search. But overall, since an agent workflow is driving towards a goal, it needs the capability to plan and to reason over multiple steps, correct itself if it were going in the wrong direction, and overall keep improving in its capabilities, which is where the self-improvement aspect comes in.

So in this lecture, as we wanted, we wanted to cover some future research areas. So we organized it as key ideas from certain papers that both cover the self-improvement side of things and just generally the efficiency side of things for intelligence. Both of which we believe are extremely important directions in the next research areas. So for the first three papers that I will cover, I'll just present the key ideas, but let me just present why those papers were chosen and what are the key ideas that they kind of point to in terms of next steps and directions.

So what we learned in the class is a lot about test-time scaling where you could improve by having multiple samples and majority voting and so on, and then train-time scaling where you can use that feedback in the inference loop and get rewards to drive that self-improvement loop. Oftentimes, this is still limited to narrow domains like math and coding. So how do you get generalization across these domains and how do you generally get those reasoning chains to be diverse enough so that you can continue to drive that self-improvement loop is something that is an important research area even now.

An important aspect of building these self-improvement loops continues to be verification. So how do you have robust verification techniques or meta-verification techniques where you verify what was going into the reasoning chain is often quite valuable. So that's another area that the second paper I will present the ideas on will cover. And the third aspect is that even though we talk about train-time scaling, the prompts that go into training these models get selected very statically and require humans to select them. So how do you break through the data barriers so that the self-improvement loop can pick the right set of data that drives that loop? So that's going to be the first part of the focus and the second part of the focus will be focused on just what drives efficiency for intelligence, so intelligence for what aspects.

So let's take a quick look at some of the ideas that might drive this self-improvement loop further in the next generation of systems. And given how fast the field is moving, maybe we'll be teaching papers on each of this next time. So the first paper that we cover is coming from multi-agent fine-tuning which focuses on self-improvement with diverse reasoning chains. The key idea of this paper is that pre-training compute and instruction tuning is bottlenecked by—so pre-training compute is a compression of internet-scale data and instruction fine-tuning often requires real human data where you need human preferences to decide what is a good model response and a bad model response. So an alternative to this is that you can use the data generated by LLMs, which is often called as synthetic data, and you can do iterative fine-tuning where you generate possible solutions and filter out the incorrect ones. This is called rejection sampling and fine-tuning only on the good ones.

Now there are many variants, STaR and so on, which can help drive this loop even better. We covered STaR in class; you can even add reasoning chains in that process. But what typically happens is that if you're using a single large language model to generate the data, then it will generate solutions that will be very similar and oftentimes the performance increase will stop after a few iterations or after tens of iterations. And typically this problem comes down to really the notion of lack of diversity. So when you look at model training at the pre-training scale, the data is so diverse because it was generated over such a long time by humans. So it is quite diverse and that helps improve the performance. But when a single large language model is generating outputs for a set of prompts, it will not have very diverse responses even at high temperatures.

And so the intuitive solution for this often means that you can use multiple agents which are specialized in some way and then use them to improve the diversity. So what this paper proposes, for example, is multiple specialized agents for generations that will then produce diverse initial solutions. So they train generation agents and critic agents. So the generation agents will help generate a lot of diverse initial solutions while the critic agents will evaluate and refine the solutions. So those are specialists as well. So if you were to look at the step-by-step process—and we don't need to go through every single detail in the algorithm below—but the generation agent will come up with an initial answer and then there will be debate over multiple rounds. So what the generation agent will do in the initial step, it will just have the initial answer, but in the subsequent steps, it will summarize and get a summary of all other agents' responses and use that also as part of generating the next response.

The goal of the critic agent is to critique this updated set of answers. So basically, if you're doing any iterations, there's an initial response from each of the generation agents. Then you do a summary across all of these agents and then the critic agent will critique this updated set of answers. So it's very similar to what you guys were already familiar with. The main difference here is that instead of getting the critique over a single agent's response, you're actually using multiple agents' responses and then summarizing across them. What this enables is that you have some form of diversity even at the generation stage before you are critiquing the answer. So you basically get majority voting for free just by having multiple agents and assuming that these are trained slightly differently, you get diversity for free.

So the generation models in this particular case are fine-tuned from the same base model and the goal there is to produce good answers given a question. So over multiple iterations, what they're doing is they're taking outputs and filtering for a match with the majority vote and then doing a safety check on this list of prompt and response pairs. If you were looking at a very naive version of doing this, you could just use different models, which is something that people do in practice as well. So they will get very different responses with different prompts. So that's the poor man's version of doing multiple generations. And the critic models in this particular paper were proposed as the critic models will basically take the updated answer and then select the best one. So they are also fine-tuned. And these are fine-tuned on a mix of trajectories where the answer is incorrect at the start and then is corrected over a course of debate. So basically the critic model is learning how to contrast the incorrect answer.

To summarize the whole process, there are n generation models and they are each producing an output. The summarization happens. You can imagine that either you summarize it with a model or you can just concatenate the responses of all the models, then there is a critic process on that and that's added to the input. And then you do a second round where all of the models generate outputs, updated answers, and then you do majority voting on top of that and again, summarize the responses and you can continue this process in the form of a debate in some ways. So those trajectories can then be used to fine-tune different critics as well.

How does this help? Why did we discuss this? So I think the key metric that we're looking for is that on the x-axis, you're looking at how many iterations of fine-tuning it has gone through and on the y-axis, you either have the negative log likelihood or you have the embedding dissimilarity. The negative log likelihood is just a proxy for performance and the embedding dissimilarity—the higher the value, the more diverse it is. So what we see on the left is for two open-source models, when they go and do fine-tuning, they are actually able to continue to increase performance when they do this multi-agent fine-tuning. So a lot of history seems more responsive to this technique. I think the right side is more interesting as to as they're going through this process, the responses continue to stay quite diverse. And this is over math as a dataset.

And overall, they tried this again in math over three open-source models. And since they were going with fine-tuning, they had to stick with open-source models. But with multi-agent techniques, they were able to continue to see improvements even over multiple steps of fine-tuning. While for single-agent fine-tuning, the accuracy collapses or doesn't continue to improve. And they also show that not just in-domain, which is math, but in adjacent domains like GSM8K. This is a slightly older piece of work, so the numbers are not that high. But even in adjacent domains like GSM8K, the fine-tuned agents actually show higher performance. So this technique actually does help even it. So it generalizes beyond just the in-domain datasets that it's trained on. So that's the first set of comments.

So what that roughly says is that if you want self-improvement, the reasoning chains that are provided to the model to drive those need to be diverse in some way. And how you generate that—one technique could be based on having multiple models or multiple agents generate those kind of chains. A second problem that's very challenging and we covered actually in the first few lectures is around verification. So it's quite hard to verify model outputs. And typically, you would just look at the outcome. So if you have a math problem or theorem proving a theorem proof, what you will look at is the final outcome. And then you have an outcome reward model. So does that match the ground truth? And then you will say that this verifies.

One of the questions that we discussed in the verification lecture is that what happens if the reasoning chains are wrong? And will that lead the model astray? Or will that cause some performance gap at the end? And we also realized that process reward models are quite hard to build. So DeepSeek-Math-V2 is a recent paper that came out, which tries to show that at least in the theorem-proving domain, it is possible to build self-verification loops which are automated. So that's a very interesting concept. Just to remind you on what I was saying, the current reinforcement learning approach assumes that the final answer will match the ground truth. And that's how you're basically creating the reward. So this has enabled saturation of multiple benchmarks like AIME or other math-related benchmarks. But oftentimes, even when you have the correct answer, you might not have the correct reasoning. And if you are actually trying to get the models to do better in theorem proving, you do require a rigorous step-by-step derivation, which the final output doesn't quite give you.

Now, the interesting aspect is that the large language models are often trained on quantitative reasoning. The proofs that they might generate are mathematically invalid. And if you ask them to verify, they will claim that the incorrect proofs are valid. So LLM-as-a-judge technique doesn't quite work here. But if you actually have expert humans look at these proofs, they will be able to look at the proof and kind of reason about the fact that they know this area. They can reason about the fact and say that, OK, this proof has issues. It's just not making sense because this next step is not following from the last step or there's reasoning gaps in the reference in the proof solution.

So what DeepSeek-Math-V2 proposes is that instead of just training the reward models, they train verifiers, meta-verifiers basically. So they get humans to identify issues in the proofs without any reference solutions. And based on that, they train an LLM to identify issues in the proof and improve upon those. And then you will find that—so you basically have trained a model which can now critique the proofs themselves. So what the final architecture for DeepSeek-Math-V2 looks like, your folks are already familiar with the fact that there's a generator and a verifier typically in the loop for building train-time scaling systems. So you have one model that is producing proofs and another model that is—perhaps verifying things, so it's identifying issues.

What DeepSeek-Math-V2 adds is this notion of meta-verifiers. So you have a meta-verifier that will review the verifier's analysis for whether it makes sense or whether there are issues with the proofs and then the verifier is trained to take these issues and then score the proofs on the scale of 0, 0.5, and 1. So the verifier will then go improve the generator and the generator will produce harder proofs which will then go improve the verifier. So basically this ends up being a loop in some way. And the other interesting aspect here is that because once you see the data for finding incorrect proofs, then you can actually automate that kind of labeling. So you don't have to just rely on humans identifying issues in the proofs once the meta-verifier starts to learn this.

So this notion of meta-verification is generally quite interesting in that verifiers can get a correct score when the reasoning chains are incorrect. For example, they might come up with fabricated errors. So meta-verification has evaluation of this analysis. Do these issues that the verifier—a verifier here is LLM-as-a-judge—do these identified issues actually exist? Does the score follow from the issues? So they're basically analyzing whether the verifier did a good job or not. And then they have experts annotate the probability of this evaluation. And just by adding this additional block, they are able to improve the quality. So it's almost like you had reasoning chains. And then now you have the verifier over those reasoning chains, which is basically saying that is this evaluation correct or not. So there is an LLM-as-a-judge which is identifying the issues in this reasoning chain. And then do those issues exist or not is what meta-verification is looking for.

If you look at the results here, just with this iterative optimization, I think all of us are familiar that a lot of the RL loop often is built on top of GRPO. In this particular case, they're building on top of DeepSeek-V3 base model. So just the verification generation loop now benchmarked on IMO problems and CNML problems. The Gemini version of that was presented by one of our guest lecturers. But this is an open-source version that was released recently. They are able to show that in 8 iterations, just pass it on, the proof score continues to climb just with that iterative loop. And then if you have best-of-32—best-of-32 means you pick the best solution out of 32 generated proofs—there you are almost getting to 42% in proof score for IMO shortlist of 2024.

So this is quite promising as a hill-climbing technique, which roughly suggests that there is some promise if you can identify the issues in the reasoning chains and actually push the model or nudge the model towards correcting them. So overall, the insights here are that the best proofs will achieve higher verification scores. And the generator can learn to differentiate higher quality proofs from flawed proofs and the self-verification as a result can basically have this better improvement loop. So verification can be a bottleneck. And this is one way to break that bottleneck.

Overall, I think if you were to try to generalize this technique in other areas, what you're basically looking for is if you have LLM-based verifiers, you need to find a way to get them to identify issues without reference solutions. That's what this paper is relying on. Then you need some form of an additional block, which is meta-verification so that it can reduce the chance of hallucinated issues that were identified by the verifier. And then you add an additional incentive for the generator to maximize quality through a deliberate reasoning. So you're basically improving the quality of the model responses here. So overall, this notion of self-verification is introduced by DeepSeek-Math-V2 and it starts to break the bottleneck in verification if you can make that more automated end-to-end. Of course, this is still limited by domains where verification is easier rather than more difficult.

So we covered through what is the diversity in reasoning chains and we discussed a bit about the verification bottleneck in the models where oftentimes to get the models to get over the bottleneck of verification, you would need some form of meta-verification techniques and DeepSeek-Math-V2 publishes that in open source. A third problem that you have to solve is this notion of what data or what prompts should I train on. So this very interesting paper that has not seen much news yet but is quite promising and interesting is that you basically have the models propose the set of tasks that they should go train on that are just at the edge of what they have learned.

To remind you, typically in the current AI training stack, you are either using human-curated reasoning traces because that's for the supervised learning stage. Or for reinforcement learning with verifiable rewards, you are expecting some experts to create the question-answer pairs. But that basically requires that if you're constructing such a model in math, then you need math experts. If it's on IMO problems, then you need IMO experts, or if it is in coding, then you need strong software coders. So as the models continue to surpass human intelligence, the ability to find more and more experts and more and more search tasks starts to be limiting.

So what this paper proposes—and this idea is still very new—is that a single model can both propose tasks and then solve them. So it almost goes to the other extreme of like we should not really need an external source of data. So basically we should not need human-generated prompts to hill climb on. But the model itself will propose a task and then it will go solve them, and so that's a very interesting set of ideas. And it is feasible in certain domains. So they focus more on coding as a domain and they effectively construct three types of tasks: abduction, deduction, and induction. And I will explain this diagram in the subsequent slides, but effectively what this is saying is that in the proposal stage, they will construct the tasks based on these task types and then come up with the reward to select which tasks make sense. And in the solution stage, they will verify and then have an accuracy reward and then that can help give a joint update on the model itself as opposed to just one-sided updates.

So what is the proposal? How is the proposal proposing tasks? That's quite interesting. So it's effectively based on the coding paradigm. So it's effectively saying that you have deduction tasks where you are generating a program and input and the environment is executing to get output. So this is your usual come up with a program and an input pair and then the environment will execute to get outputs. Abduction tasks, they're similar to deduction. You're generating the program and inputs and the environment again computes outputs, but induction tasks are different. It samples an existing program and then generates any inputs for it plus a language message describing the function and the environment then goes and executes and kind of decides whether this is going in the right direction or not. And another interesting bit that I should mention here is that the proposal will be conditioned on past—here, I'll cover that in a later slide because otherwise it will get confusing—but it will be basically conditioned on past generated examples that are explicitly prompted so that they are basically added to promote diversity.

So how do you select tasks? The proposal will get a reward based on optimizing for task difficulty. So each of the tasks that are generated are passed to the solver. And if the success rate is zero, then you get a zero reward. And if the success rate is non-zero, then you basically get this average success rate and the kind of tasks that you want to select are the ones that are not trivial and the ones that are not impossible. So there is some ability to continue to learn. So you end up selecting tasks that are of moderate difficulty where the solver will sometimes succeed and sometimes fail and this will generally help with getting the model to learn. So that's the goal of the proposal is to generate tasks for optimal task difficulty at a current set of model weights and as the model becomes more capable, then the proposal should learn to propose harder problems.

Of course, if you generate tasks, then you need to somehow make sure that these are valid tasks. So in the coding abstraction, one can run program integrity. So you can actually execute these tasks and see whether there are any errors. You can do safety checks. You can also make sure that if you run these inputs multiple times, you get exactly the same outputs because in code that's possible. So the proposed tasks before they go into the training pipeline are validated. So that's one way to make sure that the proposal doesn't just hack and come up with garbage tasks. And in terms of—there's a buffer that is kept. So for every seed triplet, like there's an identity function, like basically the input-output and the program for each of those triplet tasks, adding them to the task buffer and the proposal can sample references from that buffer. So there is some sort of a buffer that is kept along with how many times the model is succeeding or failing on this. So this is basically leading to this idea of curriculum learning that is evolving over time.

And why is this exciting or interesting? I mean, on coding benchmarks, this notion starts to let you climb despite not having much human-curated data, which humans can generate a lot of coding data, but after a while synthetic data or some—this is almost a play on synthetic data in some ways, but it's generating that in the loop at the task level as well. So what they show is that they're able to get state-of-the-art on coding benchmarks, even though they didn't have any human-curated data on the prompt side. And they are able to outperform models with tens of thousands of expert examples. Some emerging behaviors that they show are that the complexity metrics increase over time. So that can be expected if the proposal is continuing to increase the difficulty of what it's learning. And another aspect is that the diversity of programs and answers when the loop is set up correctly is improving. And the proposal is actually increasingly generating more and more difficult tasks as the training is progressing. So in some ways, it is almost like game theory where the proposal and solver are slightly adversarial, but overall, they're helping each other improve in some ways. So this is solving the bottleneck of which tasks to go train the models on.

So if you were to look at takeaways here, I think it's basically going down the path of like the task selection problem on which these models run their self-improvement loop should not get bottlenecked on human data, because otherwise you are very bottlenecked on which human experts as long as you can have some form of verification there. And you need some form of ability to do curriculum learning. So this particular technique definitely relies on that. And even though they only hill climb on self-proposed code tasks, they actually see strong performance on both coding and math benchmarks. So that's actually surprising that they are also seeing strong performance on math benchmarks, but one can explain that from other results in the field and overall larger models get bigger gains relative to smaller models. So this is another idea in the space of improving the self-improvement loop that seems quite promising in continuing to hill climb in that area.

Can I add something down on the previous? So I feel like we are seeing this same thing, like we learned about SOIL, the step-by-step RL with synthetic data. Like I feel like the findings here and there really kind of resonate with each other in the sense that synthetic data—like a model generating its own training data—can improve the model not only on the task that the generation is happening on, but also in transferability. Like in SOIL, it was on, let's do a multi-hop question answering with retrieval, but then as more synthetic data was generated and RL training was done, the model was getting better at other tool calls, like calling Python and solving math problems and vice versa. So seems like there is this like clear kind of repeated trend that we are seeing in terms of synthetic data generation by the model and generalization. And the other piece that's interesting again that kind of is the same kind of observation is that larger models seem to be better at absorbing this kind of data flywheel and also generalizing through that, especially when it comes to SOIL at least on the RL optimization side, which is like an interesting observation.

Great, great. Where would you take it from what you learned in SOIL, right? So I was gonna summarize and you can add more bullet points. I think to me to achieve true generalization and reasoning, we really need that notion of diversity in the reasoning chain. So that was the first set of comments I was making and how we achieve that continues to be an open problem. We need better verification and the more we can break that verify loop in a way where we are not bottlenecked by humans or tasks that are verified only through human experts, the better off we are in some ways, like building the world models is challenging. And then I think the third is the selection of data itself. There's only so much data that can be curated by humans for what prompts go in. So that is also quite important in the next generation of self-improving models. What else you would add there?

Yeah, I think one, just based on these kind of existing work, something that will be interesting is that how much we can push the frontier of this self-improvement only based on the verifiable domains that we can generate synthetic data and bring it back to the model. On diverse tasks, on diverse verifiable tasks, how much that pushes the performance and generalizes to other domains when we don't have automated verification. That can generally make the model smarter and smarter in areas that we can verify and minimize or remove the need for labeled data in non-verifiable domains based on that. So I think that would be an interesting kind of research area, of course, a very compute-heavy research experiment.

There are some questions that people are asking. What are some other applications of AI that don't fall into verifiable problems?

So it comes in—I can just think about some areas that verification is very slow. Like scientific discovery, running a very slow simulation in chip design that takes like a few days to run to collect one kind of reward signal, or running this chemical experiment that you actually need to like go to a wet lab and run things and see like what the output, the quality, or the reward is. Those are the domains that the verification is really hard because in RL, maintaining—we need or in test-time scaling, we need these verifiers to be almost instant, right? Or we can wait a little bit, maybe we can wait minutes, maybe we can allow like one hour, but in this RL training route, RL training, we need like hundreds or thousands of steps of iteration. We can't wait like days or like for someone, a human in the loop, to collect the reward for us. So those are the non-verifiable domains. Those are some examples of non-verifiable domains.

I think truly non-verifiable too come from creativity or any metrics where there is subjectivity in some ways.

Like creative writing, right? It's like designing an exact reward function for that is hard. Of course, we can always model that, but then the RL or the agent can do reward hacking if the model is slightly off-base, yeah.

Any other questions from the class on this aspect?

I have one question, can you hear us?

Yes, we can hear you.

You mentioned the harder areas to verify, like how are people trying to solve that? Like for example, design more like bio disciplines.

So one way to go about it is to train a model—like a separate reward model—that can predict the outcome of a simulation. So instead of actually running those expensive simulations or emulations or true experiments, you collect a lot of data on them offline and then you train a reward model that for a given input, it can predict the quality of the output and you use this reward model as your verification object like in the loop of RL optimization and so on. But of course, the generality of the reward model is a function of how much data you have and it can be inaccurate and that can cause problems. So that's one area.

So I think we talked about chip design and so on. But even before going to chip design, just the CoronaBench stuff that was done at Stanford in your lab, even there, for example, you get compiler execution. So you can see that did you generate the right code or not, you can get some metrics, but there's a class project that is actually trying to read performance profiles as you make things more complex. And that's a harder problem. Actually, even though you might be able to do design optimized kernels for simple enough things, but if you actually then coordinate all of these things together and you want to performance profile and change parts of the code that are not optimized, that's a harder problem in some ways. And oftentimes the way people go about solving it is, okay, the models can solve maybe the kernel optimization problems. So maybe I break down the problem into sub-parts and then get the model to look at each part and then have some reference solutions to look at a knowledge base. I'm literally describing a class project right now. So what I'm trying to say here is that it's because of this being a hard enough problem and the models not being at that capability, often it requires breaking down the problem in interesting ways to what the models are able to do now.

This moves to more along the lines of, okay, we can do the self-improvement, but we're now doing a lot more inference and there's a cost to this intelligence. So Azalea will cover more on how to get efficiency there.

Sounds good. Should I present myself or would you like to scroll?

Whatever is fastest, I can continue to—if you just tell me next, I will continue.

Okay, sounds good. So next please. There's some work that we did in collaboration with Professor Ray and Professor John Hennessy and a large team of great collaborators. So in this project, what we are looking for is like looking into the trends that are happening on the model side and also on the hardware accelerator side. And what we are interested in is kind of like thinking about the future and thinking about how the workloads and AI inference are gonna look like in terms of like the size of the models and the type of accelerators that we are gonna need to serve them. Next please.

So right now, we are in the mainframe era, meaning that the LLMs that we are running—we're using ChatGPT, Gemini, a lot of these larger models that we are running—all of them are being run on the cloud. We don't run them locally because these models are large. Some of them even don't have access to them, they're proprietary, and even the open-source ones for the larger models, we are still using the cloud to run these models. And at the same time, what the observation is that the demand for compute as a result is exploding, like Google Cloud had grown in something in the order of 1200X in the last 12, 20 months in terms of the compute serving. NVIDIA had a 10X year-over-year growth. And this is like crazy, right? These numbers are explicitly driven by AI. And as a result, we are gonna need something in the order of 250 gigawatts of data centers to be able to serve this demand right now. And again, this is exploding. So that means we are gonna need more and more energy kind of supply for these data centers. On the right side, we are showing this graph of the number of tokens that are kind of like from on the Google side has been processed. So in the February of last year, it was 160 billion and in October of this year, it was 1.3 trillion. So this is like one of the fastest growing compute demands that we are seeing in history. Next please.

At the same time, so we looked into like what kind of workloads are or the type of activities or AI serving demand that we are observing. And from this dataset, a very large-scale dataset of ChatGPT users, it turns out that something in the order of 77% of requests are for tasks like practical guidance, information, like asking for information or writing. And a lot of tedious tasks, it turns out that we don't need the very best frontier models to answer them correctly, rather smaller and local models can be used to address them. So on the right side, you're seeing these kind of categories of user queries, the type of user queries that exist and over time. And what is interesting here is that of course, users are gonna ask more and more complex problems from the chatbots over time, because the chatbots are getting better, but still, it's kind of like the vast majority of the type of queries that are being asked are on the side of again, the simpler side of complexity that can be addressed by smaller models rather than large proprietary models. Next please.

Another trend that is happening right now is the improvements in local inference accelerators. So the graph on the right, what it shows is 2012 until now, we saw something like 126x improvement in the GPU memory of the local accelerators. And right now, we have laptops that—our MacBooks could have something in the order of 100 gigabytes of memory. And that means that we can fit very, very large models, especially if we serve the model in a quantized version and it's like INT8 and so some of the largest models that are out there, we can take them and serve them locally. Which is like a very interesting trend that is following this other trend that a lot of user queries are addressable by smaller models. Next please.

So the question that we wanted to answer here is that what role can local inference play in bringing—redistributing the inference demand? So we have all this traffic right now that pretty much all of it is going to the cloud. Pretty much all of it is going to these accelerators like H100s and TPUs and GB200, GB300s. But the trends right now are suggesting that maybe we can do something different here. And so let's see the next slide.

So in order to look into this more systematically, we first define this new metric that looks into not only the capability or the way the models are—the accuracy of the models—but also on the efficiency side. So on the capability side, we are looking at this metric that I'm about to define called intelligence per watt. It's looking into the percentage of the queries that are being—that are at first of all, by the model in for the single-turn and reasoning type of queries. And when I say local models right now, at least in this study, we consider those are the models that have 20 billion active parameters or less. And on the efficiency side is how much useful compute we can get from these per watt from running these models on our local hardware. So in short, the definition of intelligence per watt is the average task accuracy divided by the average power draw to solve this task by the model. Next, please.

And we looked into a variety of models, hardware, work type of tasks, and evaluation metrics. We looked into more than 20 local models, as geometry and so on. We looked into both enterprise accelerators and local accelerators. The type of workloads we looked into, as you can see here, there were one million queries from source from ChatGPT and other reasoning benchmarks, such as natural reasoning, MMLU-Pro, and SuperGPQA. And we also looked into the evaluation metrics, such as accuracy, energy, latency, the compute use, and so on. And all of these data, so this massive data, we used it to understand the trends, but we are also open-sourcing all of these kind of different metrics across different types of underlying model and hardware sub-traces. Next, please.

So here are our findings. It turns out that local models are not only very, very good already, but the trend of their improvement is also very interesting. So since 2023, there was a 3.1x improvement in the accuracy or the portion of the chat queries that they could solve. This is very, very fast. And this happened in only two years. And right now, among the queries that I described in the previous slide, they could add something in the order of 88.7% of all these queries, which is a very, very large number. Another observation, which is kind of not very surprising, is that local accelerators, in terms of their efficiency, they lag behind enterprise chips. For example, an Apple M4 Max, it delivers one and a half x lower intelligence per watt than a B200. And the reason for that is that these B200s are extremely optimized to run language model workloads, whereas an Apple M4, of course, is optimized to run these AI metrics, but there are other metrics, other kind of workloads that are out there that they're optimized for. And in general, while designing these chips, the understanding wasn't that the LLMs are going to be running locally. That's why the majority of focus of chip designers are on these cloud-scale chips for running these workloads.

And lastly, there is this other very important observation that the intelligence efficiency or the improvement in this IPW metric is something in the order of 5.3x over the last two years. So 3.1x of it is coming from better models. 1.7x of it is coming from the improvement in the hardware and how the hardware is becoming more and more efficient. So both of these trends—one is that local models are becoming better and better, they can solve problems that they couldn't solve before at this very accelerated rate. At the same time, model efficiency is becoming better and hardware efficiency is becoming better. This suggests that we are heading towards this future that more and more of this traffic can be addressed or can be solved by models that we can run on our edge device, for example, on our laptop or on our phone device in the future. Next, please.

So let's go to the next slide. With that, I get back to the IPW and the future directions for that in a second. But given this observation and everything else that we have learned in this class, there are a few directions that remain open. There are a lot of interesting research questions around it that are not addressed yet and it's going to be interesting to work on. One is the foundational principles in test-time scaling and learning from this new era of synthetic data and synthetic data flywheel that these models are creating. Right now, the way we are approaching it is through RL, through collecting this data and then fine-tuning the models. But it suggests that there is more here—our understanding of, first of all, why are we seeing these properties? Like, what does it suggest from the model that as we ask the model a question over and over again through test-time scaling, what is happening that these correct answers are coming out? Like, what are the best practices to kind of distill these successful trajectories back to the model? These are still open questions and there is a very clear segue from this synthetic data flywheel kind of phenomenon back to continual learning.

Something that seems like maybe it's not really—we haven't gotten it right yet—is the following. Like us humans, as we solve tasks, as we solve problems and study and do new things, there is this continual kind of progress in how our brain develops and how we become more and more skillful. But as for the models, it seems like mostly there's this offline process of like, here is like some agent take-up kind of experiences that are generated and then maybe after some time, there's like this fine-tuning process of the model. It is not something that happens on the go. And there is this kind of a mismatch between human behavior and model behavior that a concept like continual learning could potentially answer. Like, what are the new practices that we can bring in model development that we can kind of like bring these positive experiences and learning from negative experiences and type of problem solving that the model does back into the model in a more natural way that is different from the current like asynchronous like data generation and fine-tuning paradigm.

The last piece is the infra for the high-throughput low-latency test-time scaling. So again, the way we do test-time scaling—whether it's repeated sampling, whether it's these kind of like back and forth in terms of updates that we do to the previous generations, we do tool calling, we do this and that—that is very different from the current mainstream chatbot usage, which is just like kind of mostly single-turn back and forth with the model. And that what that means is that there are a lot of opportunities for doing systems and inference optimization work for these types of test-time scaling. My lab did some of this work like works like Hydragen, Telcosaurus. And these things are going to matter again, a lot more in the future because these methods are becoming more and more mainstream.

So overall, it seems like pre-training—looking at the figure on the bottom of the slide—pre-training, a lot of interesting work in their discourse or our series of lectures touched less on that, even though Akanksha is an expert on pre-training. But we mostly focused on post-training and the test-time scaling methods. And there is this new kind of unleashed era of like synthetic data flywheel and continual learning that is happening in this kind of connection between the fine-tuning and online learning and test-time scaling that we hope that all of you learned a ton about it. But there's also a lot of interesting unsolved challenges that you can address going forward. Next slide, please.

Going back to the IPW metric and the observation about the shift, the possible shift from everything on the cloud to a lot more locally, also suggests new directions in terms of inference serving engines that are hybrid that we can kind of smoothly route traffic between our local and cloud kind of models and accelerators depending on the need and the complexity of the resources. The other direction here again is new model architectures and kernels that we can use for energy-efficient inference, especially on the local accelerators that we have. This is like an area that is much less focused on compared to the cloud accelerators. And the other important piece is that energy is going to be the most like kind of a valuable resource that we have going forward. And metrics such as intelligence per watt and better ways of our understanding of measuring energy and watt and power usage and optimizing for that again is going to be very, very important. And that is an area that is less focused on right now in terms of like the target metric of optimization, but we expect that it becomes more mainstream and more popular going forward. And I think that's the last slide on the future directions. Akanksha, you are muted.

Okay, there are some things in chat. So I'll start with the first question here. Do you think this is more pertaining to memory systems or the LLMs themselves? I'm guessing. So whoever asked this question, can you ask it real-time perhaps to put a little bit more context there? Otherwise, we're guessing the question.

Yeah, happy to. Yeah, I was just referring to a few slides back when you were talking about that continual learning like from failures and success. Do you think that we're going to see more of that in, you know, long-term memory systems and architectures or the, you know, what's designed around an LLM or the model themselves in terms of what's improved and what's worked on?

So I think the continual learning idea that might pertain to long-term memory systems, that would be the human analog of it. But if you're trying to—but before we even go there, even in multi-step reasoning, learning from successes or failures or getting the model to keep that skill set and update things in the weights or in some set of weights in a useful way. Like, can you learn a new skill? Can you learn how to learn? It's an important capability that we basically don't have right now. Like, if you can watch videos, robots, spiders, when can you watch videos of how to do something and learn how to do it as opposed to being given a lot of past demonstrations? Azalea, do you have more to add there? You're muted, if you're—

Yeah, I think, yeah, I think there are other ways also to this knowledge, like, obviously in-context learning is one way to enable continual learning. Imagine, like, one way I think about it is imagine we had an infinite context that the model could perfectly have access to every piece of it and could learn from everything in it. We don't have that, but if you had that, maybe that was one solution to, like, continual learning, right? Because it could put everything positive and negative in the context and the model could just remember all of that at the same time and reason over all of that at the same time. But we don't have that. And you go through right now, even a million or a few million, the model's, like, kind of, capability to reason over its ICL or in-context, kind of, data diminishes. And there are other ways that we learned about, like RAG, in this span of lectures. One other way that we are enabling this long context and in-context learning without changing the weights of the model, without fine-tuning the model, but instead bringing them into the activations or, in this case, into the KV caches of the model. So, all I'm saying here is that there are other ways to think about, like, continual learning without model, without model fine-tuning. One could be by just increasing the effective context length massively. And that could be another approach to memories, like long-term memory and continual learning.

As a sort of follow-up to that, like, in practice, like an implementation, like, do you think, you know, what would be easier to do, like, you know, constantly updating a model's weights or, you know, updating a memory store, right?

So I think it depends on the application, right? I mean, of course, you can argue that updating the memory store would be easier. If it's a knowledge base, if what—here, I mean, the simplest and naive version of that is that if I could keep a database that the LLM could learn to look at, then I should just go update the DB, right? But what you're really trying to teach the LLM is the ability to reason over new domains, and oftentimes having a side memory system doesn't quite achieve that. So, that's where updating the weights does the job better. And the example that I was giving in robotics actually is very, very important in there as to, like, no matter how much memory systems you add, the robot which has learned—I mean, across-the-board image generalization that you were looking at on the lecture on Monday, for example—that doesn't happen if you don't update the LLM weights just by having memory systems, right? So, that's more of a skills transfer problem.

Okay, it makes sense, yeah. Thanks so much.

Okay, there's one more question, I think, from what he's saying is that in the Absolute Zero paper, the environment seems like the data used for post-training is that a paper for agents to self-create environments. So, I mean, if you go back to the worlds where there was narrow intelligence, right, there you could basically create simulations for games. So, and those were used as environments for toy tasks. The reason environments matter in the current generation are that they are proxies for real-world tasks. So, it's not so much that there's a paper for agents to self-create environments, it's more along the lines of what is the set of tasks that you're trying to represent. If there's an easy way to simulate them, then whether you use agents to create that or software to create that, that's fairly straightforward, but is it a reasonable proxy of how the model will interact with the real world to get feedback? And for gaming, it's kind of a finite space to explore, so it's easier to represent that in code and have simulations.

Okay, thank you.

More questions from the class? You were saying something?

No.

I think there are no further questions.

Okay, awesome. Okay, how—okay, so, let's start by—let's end by thanking the class. It's been a real pleasure teaching you all and creating the content for this class so that you can learn about the latest and greatest set of techniques and the self-improving agents area. It's an evolving area. So, anything that we teach now starts to be history by the next time the class rolls around. And yet the basic techniques that you're learning are extremely valuable over time because a lot of the new stuff still builds on top of the basic techniques. So, really grateful to have worked with you all and looking forward to what you do in your projects and your posters. I'll let Azalea also thank the class.

Yes, thank you so much for—kind of the way we created this class is that we were also learning about a lot of things as we were creating the slides and we were preparing the course material because a lot of these topics are just like so fresh and so new and we were excited about them and we wanted you to also be aware of them. So, thanks for kind of accompanying us in this journey and we are very grateful to you and we hope that you learned, you learned some new skills, you got inspired in some new directions, and we really hope that this is just the beginning for you to do, to go ahead and do so many more amazing works in your research projects and your jobs and in the future in general. So, thank you so much.
