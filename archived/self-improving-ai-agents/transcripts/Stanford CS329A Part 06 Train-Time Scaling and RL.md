---
title: "Stanford CS329A Self-Improving AI Agents | Part 6 | Train Time Scaling/Scaling RL"
source: "https://www.youtube.com/watch?v=yVnmHSAy3ck"
author:
  - "[[Stanford Online]]"
published: 2026-08-03
created: 2026-08-21
description: "Want to dive deeper? This curriculum is covered in the following online courses: - Agentic AI professional education program: https://learn.stanford.edu/agentic-ai-2026.html - XCS329 graduate course: "
tags:
  - "clippings"
  - "cs329a"
  - "agents"
---

Welcome, everyone, to the sixth lecture for CS3-9A. Today, we'll cover train time scaling, or scaling RL. So far, we have gone through several topics related to test time scaling, feedback using tools and code, and then also robust verification. Today, we will see how we can close the loop to improve the models even further.

Okay, so today's lecture, we will actually cover three papers. The first paper will cover how you can boost the reasoning capabilities of the models by putting rationales in problems. This paper is called "STaR." It was actually done by an author at Stanford. The second paper will cover DeepSeek-Math, which talks about mathematical reasoning and how that can be boosted in language models. And then the third paper is more focused on reinforcement learning and how, when you have to do reasoning with long reasoning chains, you can stabilize those kinds of reinforcement learning algorithms.

To ground and motivate a lot of the papers that we'll present today, if you look at a benchmark called AIME—AIME is a benchmark on mathematical problems which tests for mathematical reasoning. It's very similar to, say, more complex mathematical reasoning problems than what was in the MATH benchmarks. We used MATH benchmarks earlier this year, and that one is already saturated and contaminated in most of the models. Basically, most of the models have been trained on it, so we had to change the benchmark. So this is AIME 2024 and AIME 2025. The benchmark that you use in Homework 1 is a recent benchmark that has mathematical reasoning problems.

If you were to evaluate GPT-3.5, which is a 175 billion parameter model—or that's what it's believed to be—you'll get almost 5% accuracy. But if you were to take DeepSeek-Math, which was using train time scaling on a 7B model, it gets 51.7% accuracy. And with additional tricks, it's able to even get 60% accuracy. And if you apply the third paper that I will talk about, you'll get 50% accuracy. So in the prior work or in the first lecture, we were saying that as we scale the number of parameters in the model, that's when the model capabilities go up and the model accuracy on benchmarks goes up. But what we are seeing in this particular slide is that you can achieve a pretty high performance on reasoning benchmarks with smaller models. So how did these smaller models perform and how did they get really high accuracy? That's something that we will explore in this particular lecture.

Some of the tricks that are being used to achieve this are called train time scaling. Typically, if you try to do reinforcement learning on reasoning, it will not work well. And a lot of the time, the reason it doesn't work well is because the implementation details are hard to get right. The key insights that we will study today are that if you have a model—earlier, you were just training it on the internet. Pre-training, the technique of model training, was training the model on the internet. If you allow the model to learn from its own outputs by filtering cleverly, you can improve the models even further. Hence, the class is called "Self-Improving AI Agents." The amount of compute you invest in training the model on its own outputs can substitute for the model parameters. Also, some of the earlier efforts where the amount of compute used in training smaller models, if you could increase the amount of data, has been shown to help improve certain benchmarks.

The third point is that when you're doing reinforcement learning as opposed to supervised learning, you have to really be careful about details of the implementation because as you scale up those algorithms, the small fixes can be extremely important. The three paradigms that I'm talking about here are pre-training, which is basically training the model on the internet that we talked about in the first lecture and we're not covering that much in detail. Then the next technique would be fine-tuning, which is how you get your chatbots where you're basically fine-tuning or doing reinforcement learning with human feedback or RLAIF on preference data from humans. And then the test time scaling, which was some of the techniques that were covered in previous lectures—those are more techniques where you're basically doing some sort of, say, majority voting or inference time sampling and then combining the outputs from how you sample from the model. For example, in Homework 1, you are evaluating majority voting, then you are evaluating some other techniques that combine and evaluate the errors in what the model is producing and then using that to improve the model. So test time scaling is mostly inference-based techniques. Now, if you take what the model output filtered after applying this test time scaling and then use that to fine-tune further, that's roughly what train time scaling is. So if you take nothing away from this entire lecture but you just remember this loop, then you kind of have learned the basics of what train time scaling does. Questions?

"How do you know what is like the good balance to distribute your compute into train time scaling versus test time scaling?"

Great question. Let's hold that question until the third paper. So definitely ask that question again. Roughly what happened last year was that the reasoning model—this is a graph on o1 and o1's accuracy on AIME. Its pass@1 accuracy on the Y-axis and then the amount of compute on the X-axis in train time is shown on the left graph, and test time compute is shown on the X-axis on the right-hand plot. What is happening here is that as we are increasing the test time compute, as we have already seen, the pass@1 accuracy should go up. This is a log-linear plot, so more inference time compute means better pass@1 accuracy. But what they're also showing is that if you can increase the train time compute, then you can also improve pass@1 accuracy. So what o1 showed for the AIME benchmark was that by increasing both the train time compute and the test time compute, you are able to improve the accuracy on the benchmark. It allows you for basically a circular loop where you can generate model outputs and then feed them back to improve the model on this particular benchmark.

Now, why does this work? Math is a domain in which there is verifiability. We had a whole lecture on robust verification. You have the ability to know which outputs are correct and which ones are not correct. So you have some ability to actually choose the correct outputs that can then feed into train time scaling. This works better in domains where there is verifiability. Typically, why does reasoning start to help? This is a repeat of some of the slides, but I think it's very important to remind folks that all the reasoning models in the o-series or Gemini Flash Thinking or their equivalents in other frontier labs—basically reasoning allows us to solve difficult problems because when you're solving a difficult problem, you will not just spit out the output. Even in a chain of thought and math problems, when you were asking the model to think step by step, it allowed a certain amount of tokens to be dedicated to spelling out the reasoning and step-by-step process.

Thinking models use a chain of thought when they're attempting to solve the problem, and what these tokens might get dedicated to are patterns like problem analysis, where they might actually try to understand what the problem is describing; task decomposition—I'll show you an example of this, but where they try to decompose the task into subtasks; self-evaluation—they might actually try to look at the answer and then be like, "Okay, this is not looking correct." They might actually choose to backtrack and recognize their own mistakes and then backtrack to a previous step where they redo the problem. And they might try some kind of different approaches. It's like a somewhat parallel search where they might actually try multiple approaches at the same time if the current one is not leading to a solution, so it needs to have some sense of whether it's the correct solution or not.

Here are some examples from an earlier version of the model in the o1 series. Here the user is requesting a bash script that will represent a string with this format and print out the transpose in the same format. It's taking a matrix and printing the transpose. What you see in the output is that the model first tries to understand what the user is asking in terms of input and output formats before it tries to solve the problem. That's more of a problem analysis kind of pattern. Another example for the same problem: you see that it tries to decompose the problem. Once it understood the problem, it's saying that the approach should involve, "Maybe I should parse the input string, then build a matrix, transpose the matrix, and then output the matrix in the same format." It's breaking it into multiple steps. While for this particular problem this might be an overkill, in more complex problems this might be a better way to solve the problem because you've decomposed the problem into smaller steps.

And then self-correction. This is a different problem from chemistry where it's asking for the pH of some solution, and the model starts to compute the pH value, but then it suddenly has this notion of, "Wait, the correct formula is different." It basically goes and changes the formula that it was going to use. So even though it's pulling from the knowledge that the model already has, it's actually correcting itself. It has the capability of self-correction. And how does this help the model? As I was trying to explain before, in domains where you have verification like computer programming or data analysis or mathematical calculation, the win rate over GPT-4o, which was not a thinking model, you'll see that it's greater than 50%. Certain domains benefit way more from these train time scaling kind of techniques as opposed to, if you look at personal writing or editing text, there is not as much improvement in terms of win rate. Win rate means like if you were to give humans output from GPT-4o versus thinking models, personal writing and editing text may not see as much gains, but domains which have the ability to verify and close the loop see more gains. Questions?

In this particular plot, it does look like that. There is no intuition around these things as to like one should be better than the other. I mean, this is one instantiation of where things were, and I just wanted to show you the curve. I copied the plot. Their graphs are not always right, as you might know from their latest release.

Okay, so let's go back to the fundamentals, right? What is it that we're trying to answer? I think the fundamental question we're trying to answer is that in test time compute, you can basically—if you go back to this plot—test time compute is very cheap for someone to put because you've trained a model and then you can go and do multiple inferences. And if one of the answers is correct and you have a good verifier on your end, then you can basically scale compute infinitely. In fact, I think next Friday I'll go through some papers, including AlphaCode, for example. What you'll find there is that if you can basically make your search infinite in some ways, then in test time you basically have a solution that will be correct because from test time scaling, we do know that there is a correct solution if you repeatedly sample, right? Train time scaling, the challenge will be that you have to scale it correctly, which we'll cover in this lecture. And then the second bit is that this closed feedback loop has to have enough successes for the train time scaling to work well. You will see that in the papers that I'll present now.

There's a caveat to what you're saying. Test time compute works well in benchmarks where the verifiability is robust. Otherwise, you do need to get the model to start to reason better because you need some verification loops. Both serve their—I mean, this is something we'll discuss in the lecture—serve their goals. So like if you prefetch what I'll present by the end of the lecture, when you look at a pre-trained model, what it is good at is it has a certain level of capability in, say, solving mathematical problems and solving reasoning problems and reasoning about finance or legal or whatever based on whatever domains it has seen data in already. But then in inference time, you're basically getting it to reason over multiple of these traces and then picking one. If you know and have a good way to verify the solutions, then you pick one of the solutions. That's like saying, "I'm going to throw spaghetti at the wall and I know where it should land." Otherwise, with train time scaling, you can teach the model and boost the pass@1 accuracy so that there's more likelihood that it will emit the correct output. We still need verification in the loop. More questions?

"So when you're fine-tuning your model to be able to solve difficult problems better, does it have any regression on solving simple problems? And if so, how do you balance that?"

That one also needs to be—let's keep those questions. I like all the questions, but we need to get to the first paper and then we can ask that one. Typically, there shouldn't be a regression on solving easy problems if you're solving difficult problems unless you basically go into reasoning chains that are completely broken in some ways, so repetitive reasoning chains, which is called as overthinking.

Let's cover the first paper first where we are basically teaching the model how to reason. We are boosting the reasoning capability of the model with reasoning chains. Let's take a look at how we can do that. We already covered this earlier and we showed this in the first lecture. Chain of thought in large language models provides both interpretability and, if you ask the model to show its work step by step, then it provides a boost to mathematical reasoning, and we saw several benchmarks, common sense reasoning, and so on. We want to basically get the model to have strong step-by-step reasoning capabilities along with solving any problem.

What is challenging with existing approaches if you were to try to get this? No internet-scale data really has reasoning steps; most of the time there are very few internet sources that have large-scale reasoning steps. And then if you try to manually annotate reasoning steps, that's very expensive because you literally need humans to annotate those reasoning steps. If you try to automate this process of generating reasoning based on known solution patterns, then that also works only in very, very specific domains. And then if you try to few-shot prompt—few-shot prompting means like if you give it a few examples of "here's a problem, here is a reasoning chain, here is an answer"—then that still underperforms models that can be fine-tuned on just larger datasets. Basically, a small amount of reasoning examples don't quite cut it, and you can just have a larger dataset without reasoning chains.

The key insight that STaR has is very simple. It will start with a small number of examples that have reasoning steps. It will generate solutions—say 10K problems, 10K is a representative number—and then it will only keep the ones that have correct answers, but it will generate these problems with rationales and it can fine-tune on those and it can repeat. It can repeat this loop. What's new here compared to what we were doing in test time scaling is it's only keeping the ones that have correct answers. Now, why did it not produce correct answers on the other ones and what can we do with those particular set of examples? That's the other interesting bit.

If we just fine-tune on the correct examples, the model is not able to learn how to solve new problems. Let me cover this slide and then we can come back. There's basically no signal. So what we do is we give the model the answer and ask it to explain backwards. It's almost like I have this question, I asked the model to produce the answer, but now the model did not produce the correct answer. So I give the model the answer and then I ask it to produce the rationale. For example, the answer is 42, the human says, and then the model generates the reasoning, and then you can fine-tune the model on the problem, rationale, and the answer, but don't show it the hint as if it was solved directly. This helps you expand the training set to include more difficult problems now. Basically, it allows you to bootstrap the reasoning capabilities and not just be limited to easy problems that the model could solve earlier. You can bootstrap it iteratively.

You basically generate reasoning attempts. You learn from successful reasoning paths where the quality of the reasoning paths is entirely based on things being correct. And for failed attempts, you're basically rationalizing them by saying, "Here is a hint, please generate the rationale." And then you're using these generated rationales as the training data. That's basically the algorithm that they use in STaR. It's a very bare-bones and you can almost call it an off-policy reinforcement learning technique. It's a very bare-bones way of producing reasoning techniques in a model.

The assumption that is being made in this particular case is: how do you know something is a good rationale? They're assuming that if the model output is correct—which in math they found that that was generally true—the correctness of the final output is a proxy for the reasoning quality. And so they're filtering incorrect answers. Basically, they're assuming that that will be filtering out lower-quality reasoning chains, but there might be scenarios where they are not learning from incorrect reasoning chains as a result. But they get high-quality training data. And then the second bit is that they assume that the language model can generate valid reasoning paths when it's given the answer as a hint. It kind of assumes that if I show the model the answer and then ask it to show its work, it will be able to do so. And then finally, there's some amount of assumption that the initial language model is strong enough to bootstrap from few-shot examples. If the class of problems is way outside the capability of the initial language model, then it will not make much progress on this. And this is where the iterative aspect starts to become important. I was seeing a couple of hands, so let's take that one by one.

"I was just wondering, so do they start with like a math dataset that has like the correct answer? It just doesn't have reasoning, right? That's how they know what the correct answer is."

Yes. They know the correct answer. Anyone starting with a benchmark, basically, yes.

"Is there filtering that happens for step three? Are we worried that we give it the correct solution and then there's like issues of the reasoning just to get to the answer?"

They actually don't filter in step three, but there are follow-on papers that do filter in step three. I mean, you can do some sort of process reward model on top of the reasoning chains and then say which of the steps makes sense or which of the steps don't make sense. This particular paper was one of the first attempts of this process to show you papers as they evolved as opposed to what's the latest paper here. But yes, that might be another project idea: how do you evaluate the quality of the reasoning chains if you don't want to depend on just the final outcome as the proxy for correctness? Yes.

As I told you in step four, the assumption is being made that the benchmark on which this is being done is essentially where the problem is not too difficult. It is within range, or some subset of the problems are within range of the language model. And typically you can—so that is an assumption that is made. And then when you do it iteratively, the model might get better at a certain point that it can start handling these other problems.

I think what I'll show you by the end of the lecture is that learning from negative examples has not been nailed. Learning from positive examples has been like, if you have any non-zero reward, then you have an ability to close the RL loop, which is not happening yet in this particular set of examples. So you do need to be able to close the loop in some way. If you have negative examples, there are some papers that have tried to learn from it, but that's not something we'll cover today. Yes.

"I have a similar question, like how do they verify the quality of the generated rationales after giving the hint? Since the model already knows the answer, it might generate incorrect reasoning."

That's a great problem. So you're saying that in step three it might generate wrong rationales. Then it will learn wrong reasoning from correct attempts. Yes, that would be a problem. In this particular case, they don't have any further filtering on top of rationales. So there will be no way to avoid that in that case. You can have humans look at the reasoning chain. It could be tedious, yes.

Okay, so going through this in a little bit more rigorous detail. You start with a prompt dataset where you have the questions, the rationales, and the answers. This prompt dataset will be small compared to the amount of data that we want to train on. For example, this is the multiple-choice question: "Where do you put your grapes just before checking out?" There is a rationale, and then the correct answer is, for example, B, which is "grocery cart." Now, you have a training dataset which is quite large, of examples that have questions and answers. This would be, say, if you have a benchmark, you might have a train subset of the benchmark and a test subset of the benchmark.

What STaR will do is it will start with this rationale dataset and training dataset. It will first few-shot prompt the model with the rationales in the rationale dataset. And then it will get the model to produce the rationales and the correct output. So this is on the training dataset. Now it's few-shot prompting, but it's supplying problems and questions from the training dataset. And it's checking if the answers are correct by the language model. It will collect these set of rationales that it can use, and then it can fine-tune the language model on these correct solutions.

You have rationalization, which is what STaR does. If the answer is incorrect, what you will do is you will add a hint. You will be saying, "Okay, here is the correct answer," and you will essentially generate the rationale by looking at this hint. And then you will use that to add to that dataset of rationales that led to the correct answer. And then you can fine-tune on top of that language model. So you basically have these two sets of rationales that then you can fine-tune on top of this language model. And you can do this process iteratively. You don't do it just once. You fine-tune it now; the language model perhaps is able to produce better reasoning chains. So now you can again start with your training dataset, and whatever was not correct, you can redo this process.

Their experiments were on a small model, which is an open-source version of GPT-3, a six-billion parameter model called GPT-J. They had a pretty small warm-up, and then they kept their constant learning rate. They basically had a certain number of iterations of the outer loop, and then they increased the inner loop, the number of steps in the inner loop on each step. One of the comments the paper makes is that they want to have a slower start in the training time, and then they want to slowly increase the number of things that are happening in the inner loop.

The datasets that they tried this on were GSM8K—which is just math problems that you have seen, great school math problems—then CommonSenseQA. CommonSenseQA is essentially problems that you would see related to day-to-day stuff. You will have these multiple-choice questions, and then you have these answers, and there's rationalization that looks like this. And then arithmetic problems, which basically was some sort of an addition problem on multi-digit addition. The last one was synthetic data, where they generated some of these things themselves.

In terms of results, some of the results that stood out in this paper was that just compared to, say, supervised fine-tuning on a math benchmark, they could boost with this STaR approach to 51.7. But they only used 70 to 87% of the data in terms of—so they had to throw out a whole bunch of data that did not have correct solutions. Another challenge is that it's not really true RL. If you basically do multiple iterations of this, it starts to plateau after a while. RL has its own set of challenges, but here it also starts to plateau. So you have to really pay attention to how many iterations you can do. There's a lot of tweaking there that you have to do.

The other interesting set of results here were that this was on common sense reasoning. They actually looked at—this was a question that several folks asked here—they actually tried to see the rationales for CommonSenseQA. They showed it to human raters, and they asked human raters to say how likely they are to prefer the rationales from STaR versus just otherwise. They compared STaR-generated rationales versus the few-shot rationales in the rationale dataset. What they found was that generally, the qualitative analysis showed that their rationales are pretty reasonable. Now, CommonSenseQA, as you remember, is natural language, tackling daily problems. As a result of that, these rationales seemed pretty reasonable.

The other aspect that they noticed was that STaR in general—if you use direct fine-tuning, you basically do get a certain level of accuracy, but STaR with rationalization gets that accuracy while requiring much less data compared to what you would need for fine-tuning. So rationalization helps you get to an accuracy without using—86% of the data is used, but you can basically get to the dev set accuracy of 72.5% compared to using a lot of training data. This was a very small model. This was for science of life that was shown for reasoning.

In terms of mathematical reasoning, this was an interesting one on GSM8K. What they found was that the use of rationalization actually did not improve performance. So STaR by itself was not helping in this particular case. What you see is that you have GPT-J—basically, it improves the baselines, but it's basically not helping too much. If you directly do fine-tuning on good data, then you can actually improve it similarly. One of the reasons that is the case is the number of calculation steps that were used by the model themselves. If you force the model to use chain of thought versus you provide these rationalization steps, it's actually pretty similar. In some ways, it was not helping if the problem is too simple. The whole notion of getting the model to reason, if the problem is too simple, was basically not helping. GSM8K for GPT-4 was very much within capability. Similarly for GPT-J, so there was not much improvement here relative to. But it did show that direct fine-tuning versus STaR with rationalization, you still had some gains.

Some takeaways here are that the whole notion of rationalization conditions on the fact that you have an answer and then you can essentially figure out what rationales would be good. The outputs without rationalization are examples where the model is already quite confident. But then if you can look at the answer, then in certain cases, it might actually do a better job producing the rationale. If you do few-shot prompting, you can basically really help the model in how it should produce rationale. But the style of rationale might actually influence the model in certain ways. So you do have to—prompt engineering in this particular case might bias the model in certain ways. Effectively, there are certain challenges: one challenge is that the rationalization quality has certain amount of effects, and the second bit is that if you're few-shot prompting, then you are biased by how the rationales were formatted in your few-shot prompts. That might affect what kind of rationales the model would produce and what you're training on.

Overall, I think this kind of gives us a sense that in the model space, if you basically bootstrap the model to generate rationales, you can get high sample efficiency while not having to get humans to generate these reasoning chains. And you can apply it to many problems, like symbolic problems, natural language, mathematical reasoning. The limitation in this overall technique is that you don't have a very good evaluation for rationales for most tasks because you basically have to have a human evaluate them or you need some sort of a process reward model to evaluate them. There might be true negatives and false positives. You're just filtering based on correct answers, so you might have some invalid steps in between which might cause problems. Yes.

"Right? Because if you want to fine-tune a small language model, you can just use a big model to generate these training rules. I don't know how to answer that question. So you're saying that you don't have to rely on a small model to generate you as you have any sort of data automatically. It just seems a big model and you can train new stuff. But I'm wondering the main benefit you are actually getting training out from nothing. All this shows that you might train some of these basically defaulting."

That's a fair point. You're saying that you don't have to rely on a small model to generate rationales if you have a big model. The goal for a lot of the work that we're discussing is the ability to—I mean, they're showing the solutions on small models, but at the end of the day, what we want to be able to do is take the model, take its outputs, and then use that to improve the model. You can always take a more powerful model and use its outputs, and that's like a distillation process.

"So what I mean is that, if I want to improve the performance of a 7 billion parameter model, I would use this because I didn't get that high-quality data from distillation."

We are not covering distillation, but that's a fair point that you can use one of the frontier models and distill things, but this is more an attempt to build it from scratch and for you folks to understand what might that take. Yes.

"So when you decide to make a choice to post-train with SFT versus RL, and is there any different implications of how much data or compute is needed?"

Hold that question. I'm covering that. That's part of the lecture.

Just to cover, in STaR, you basically were just using the correct output, which is like a proxy for a verifier—like you already know the answer. If you basically also add a verifier in that whole loop, then that would be called V-STaR. There was a paper that basically had a generator and a verifier training in a loop. And then Quiet-STaR was essentially adding an internal thinking. What it was doing was essentially, instead of having reasoning steps be in the language space, it was actually putting reasoning steps in the latent space with MLP. Why have them in English when you can have the model thinking entirely?

Before I move further, actually, there have been a lot of questions. I've only picked one of these questions, but let's look at question two: "What bounds the performance of STaR as an approach?" You can talk to someone for a minute and then let's chat as a class.

Let's come back as a class. So what bounds the performance of STaR as a technique?

"So we were thinking like there are some logical steps that are kind of more complex than others. And you wouldn't expect STaR to be able to make any logical leaps that are not in its training dataset. Like the rationalization should all kind of be of the same bottle as what it was trained on. It just allows you to get more rationalization potatoes."

It doesn't let you figure out new things is what you're saying. Okay. Any other takers?

Say that loudly. What you're saying?

"Yeah. So interesting. Like this. All of us think it can be found in advisor by models. Do you reason me? Because I think the model has been given the hands that's also not going to work out because the rationalization would show up at all. Wrong sample. That opens the question. What leads to this?"

I think some answers make it inherently easier to rationalize than others. For example, if the final answer is like 225, you probably know that at some point there's like the multiplication of 15 or something similar to that. But maybe some other numbers are not as straightforward. And I also think that if there's a lot of hard problems that you don't know the answer to and that force you to rationalize, you probably need more data so that you can form more generalized reasoning to that chart. Okay.

And that is in the right direction. Yes. I think all of these answers are fairly valid. But I think this is worth keeping in the back of your mind as we move into RL because a lot of the magic in this particular domain is like: what can the base model do as we're building on top of that? We'll come back to that.

Okay. So the second paper that we'll cover is DeepSeek-Math, which will focus on mathematical reasoning and will actually use a form of RL that it proposed that has become quite popular. We already looked at this graph earlier where we were basically showing that in the math domain, if you do train time scaling, you can do much better.

What this slide is showing is that if you look at the performance—this is an earlier benchmark than AIME, it's actually a simpler benchmark than AIME, it's called the MATH benchmark—and you're basically looking at top@1 accuracy. As the model size is increased, typically the accuracy on this benchmark has gone up. But then suddenly when DeepSeek-Math came out, a 7B model was doing really well. And the main leap that they made was that they were able to do the reinforcement learning part with just in the train time scaling loop correctly. So we will see what led to that innovation.

The first thing that they got right was that typically when you try to improve on math—and this was something that was done on top of PaLM as a paper, a model, there was this paper called Minerva, which improved the PaLM model on STEM—what they did was they trained on a lot of science and math data, typically like arXiv data. What DeepSeek did was it was like, "Okay, actually training on arXiv papers is not the trick." They actually trained on Common Crawl web pages, but they started with a DeepSeek-Coder model. They said that if you start with a model that has already been improved on code reasoning, then it will actually do better. They did a really good job at curating content from Common Crawl pages on math. They found that often arXiv papers in some ways don't have an adequate amount of math content that would give us coverage. OpenWebMath, by curating that and mining it, they were able to actually get better coverage across math domains and get a much larger yield in number of tokens. That was their first exercise in priming the model before they take it to RL. This was related to one of the comments that were made: that if the model is not strong in a certain domain, you do have to improve its capability in that domain. This is that step. They basically got coverage across a lot of math domains, showed it a lot of high-quality data in math, and they started from a code pre-trained model. They actually showed that arXiv training was naturally helping, but code to math significantly helped because it allowed the model to reason better and do tool use better. This was actually the first time that this was shown: that if you start from a coding-based model, you could do better. And then the other thing that they showed was that the curation of data really helped in this particular case.

Now the second step that they did was: okay, now that you have trained the model on math using supervised fine-tuning, what do you do next? Basically, this ends up being a memory problem. If you try to do it on larger models—a 7B model is fine—but if you try to scale up RL, you have to keep multiple policy models. You also have to learn a critic model and a reward model. Typically in the RL loop, you have an old policy, a new policy that you're learning, you have a critic that's basically giving feedback, and then you have a reward model that's assigning the rewards. Keeping all four of the copies of models starts to become challenging.

What they did was they proposed a new technique called GRPO, where instead of having a critic or a value function estimate things, they basically used some sort of a generalized advantage estimation that I'll show you in the next slide. And then you only have three copies of the models. PPO, which needed a lot more memory—instead of that, GRPO essentially could use just a group for the baseline. The way that works is that for each question, you sample a lot of the answers and then you score them and you normalize the reward. This is the score that you gave for each of the answers using the reward model. And then you normalize the reward as (reward - mean of rewards) / standard deviation. This becomes your advantage, basically. Typically, if you look at the way DPO works or all of those techniques, you're basically going to compare the rewards. By doing a (reward - mean of rewards) / standard deviation, you're effectively providing a comparative technique across rewards. The reason this made sense was because reward models are going to essentially be trained on comparison anyways. So by providing the group context, you can save memory and now you can start scaling up RL and your advantage function in this particular case essentially. I didn't add the full formula because I just wanted you guys to have intuition. You could just take the reward, subtract the average reward, and then divide that by the spread of rewards and you can get an estimate of the advantage.

On math, they were able to go from 46.8 to 51.7. This was actually the first open-source model at 7B scale that crossed 50% without having a critic. This is a much earlier paper, but then this gives you a sense of the technique that they used. Further, what they showed was that if you look at different variants of RL, effectively what you are changing is either the data source—I showed you how they curated their data—and then you're deciding how to compute the gradients. Gradient depends on the gradient coefficient and the probability of the output given the question. The gradient coefficient computation changes in different algorithms. Their estimate of the gradient coefficient was coming from this group estimate or group baseline, which worked well for a single-step problem where you're just trying to get the answer.

If you compare approaches like STaR, where you basically are just generating things once and if you get a correct one, you basically get a reward of one if it's correct and zero if it's wrong. Or if you do online rejection fine-tuning, you are still generating things online, but you only get a reward of one if it's correct and zero if it's wrong. You basically reject anything that is incorrect. GRPO is generating rewards online with the reward model; it's basically getting an advantage function because you're doing multiple samples and then you're giving them a score, and then the advantage for that score comes from this group baseline by computing the mean and then dividing it by the spread of rewards.

Now you can ask me the question that you were asking me earlier about the difficulty of the problems.

"Oh, the difficulty of the problem that I made three rational center questions."

Yes, so I'm curious. If you are updating the model with fine-tuning for difficult questions, you are essentially updating all the weights. So isn't that going to potentially theoretically have any regression on center questions or other metrics?

This is the data that I wanted to show you. Effectively, if you're basically trying to close the loop, what you're trying to figure out is how much reward you are giving. Simple problems effectively might equate to you're going to get a reward of one anyways and you're never getting a reward of zero. With GRPO, you're getting some form of a distribution of rewards that I was showing here. If you don't have any distribution in rewards, then—so effectively what you want is some form of a distribution of rewards across the set of problems you are showing. If there is not a distribution of rewards, there's nothing for the model to learn. If all the rewards are zero or all the rewards are one, then basically the normalization doesn't work. Does that make sense?

"Yeah, yes, but the shortcoming of this particular method will solve in the next paper."

But this reward is specific to like this one particular feature that we're trying to train and not like the basic overall holistic benchmark that we trained for the previous model report.

I mean, it's basically taking problems and then computing answers, right? Across the model, you basically are showing problems that are all very hard, then the model has no capacity to learn because it will basically have no reward at any point. It needs some hill-climbing signal, right? I'm not showing you a formula; I'm intuitively explaining it to you. Yes.

"Yeah, actually I just want to call it not that easy. I think my interpretation of asking the solution to not using any original paper is like, that's why there's a KL divergence term. So essentially it's like, it is a valid concern. I think as it's like, if we're updating the weight to get problems training in the far, and maybe we can get a really good answer to these benchmarks, right? If we got low to give you an optimize on all these like simple benchmarks that are here in the first train, right? So I think in addition to the normalized rewards, you also have some like KL divergence in the policy."

Okay, that's a fair interpretation. That you have KL divergence so you don't deviate from what it could already say. I was interpreting it more along the lines of: how do you have a good distribution of problems so that the model has the right hill-climbing signal? But I think what you're saying is that if the model could already solve something, then KL divergence solves that part of the problem. Thank you.

"So if you were trying to post-train so that it has like a new teacher, do you really have to update all the weights?"

No, I don't think so. There are recent works that have shown—there are some blog posts that have been recently shown—that you can do it with lower updates and so on. So I do think that either KL divergence or some subset of features could solve that problem. I think the reason this is interesting is that you want—you want perhaps that these kind of techniques allow you to improve the model overall. Yeah. More questions?

I mean, you can also have zeros and ones there and then you can average things. What I was showing you, you are training a reward model that is basically going to give you a score for that feedback. Yeah. If you only use zero and one, that can be a proxy, but then you can do some averaging.

Okay. So what this roughly tells you is that if you do basically the online reinforcement learning loop and you sample from the current model, that beats what we were doing earlier in the STaR approach. Overall, what they showed was that you can improve the majority of the—if they had 32 tries, it was improving the majority@k, but it was actually not improving pass@k. If you were basically looking and plotting pass@k, say one is correct if you sample 32 times, then that was not what was improving in their paper. What was improving was that the majority of the solutions become correct. That's what was improving in this particular paper. So the model actually became more consistent, not fundamentally smarter, was what they showed.

Now we'll cover the last paper called "DAPO," which is basically going to fix some of the problems with respect to reasoning on harder problems. If you naively scale up GRPO and want to do this on even a Qwen-32B model, which is very easily accessible, you would get 30% on the AIME benchmark. Here I will show you a lot of technical terms, but I will try to explain to you intuitively. Some of the challenges in just doing this on Qwen-32B end up being like the entropy of the model collapses, the model can become too confident, the training can become unstable, and the response length—the model output length—can become uncontrollable, like it can explode uncontrollably. But DAPO did get 47% on AIME. DAPO as a published piece of work tries to make explicit what were the techniques in RL that were not covered in GRPO.

Some of the techniques that they talk about is that in PPO you have to do—so you're basically trying to do some sort of exploration in your standard PPO algorithm. The standard PPO clipping treats the increasing and the decreasing on that very similarly. So basically if you have a low-probability token, it can only hill-climb so much. High-probability tokens can also get clipped, so the exploration can collapse. Basically, the clipping function in the PPO algorithm needs to be asymmetric, and that will allow bigger increases. That was the first thing that DAPO found. In terms of graphs, what that roughly meant was that if you did clipping, then your accuracy was much better. And if you could allow clipping higher, if you could allow asymmetric clipping, the purple plot shows you that you could achieve much higher accuracy and your entropy on the right side stayed more stable and did not collapse, while if you did not do asymmetric clipping, then your entropy has collapsed. Entropy is a proxy for how much exploration is possible in the model.

A second very interesting technique which actually relates to what we were talking about earlier was something called as dynamic sampling. What I showed you for GRPO was that, okay, you go and sample 64 solutions and then you come up with a distribution on them and then you take a group baseline. What DAPO said was, "Okay, well, how about we oversample? How about we don't just use 64 samples? How about we oversample?" And then what we want is that we will filter out things that have zero reward and one reward so that what we want is basically a nice distribution so that most of the solutions have some sort of a signal. They filter out all-correct and all-wrong groups and they keep only some subset between zero and 64. They're basically doing dynamic sampling because they don't want to waste their gradient. Your advantage should not be zero. If your rewards are all correct or all wrong, then those groups are filtered out and you essentially want to keep only those groups that have some signal. This maintains an effective batch size for propagating the gradients because you're not computing gradients from all-correct and all-wrong anyways. Questions?

"So you have a match of questions when you are training and then you're filtering out questions."

These samples are quite small and I'm wondering if it's because there's a very limited amount of AIME questions out there and whether that was enough.

This is more an example here. You can choose how many samples you pick, but at the end of the day, this is mostly to showcase what was possible from their paper. AIME—yeah, this will be benchmark-specific and the difficulty of the benchmark and the capability of the base model.

The third bit that is worth noting is that the reasoning chains have a certain amount of length that matters. If you are basically doing a sample-level loss—sample-level means that for each question-answer pair, for that sequence you're computing a loss—then each answer is basically counted globally. In certain cases, if you have a very long garbage answer in the model output, then that basically gets the same weight as a short group that would answer. They effectively suggest that you should have some sort of effectively length penalty, but they want to shape based on the length. So they put a token-based loss as opposed to putting a sample-level loss, and that gave them some gain. In particular, what they were controlling for is both the entropy and the mean response length. Effectively, if they didn't have a token-level loss, they were noticing that the entropy was growing and the response length was also growing uncontrollably. They wanted to shape that better by putting some form of a penalty there. So they put token-level loss for that.

And then finally, there are certain cases in which the model encounters a hard problem. It basically gets truncated at the end because it was thinking and it was basically producing model outputs, and these truncated reasoning chains can generate a lot of noise. They basically put a gradual penalty in the tokens to deal with that, and that allowed the training to become stable instead of adding noise. Effectively, the generation entropy, instead of becoming unstable—once the model was learning to generate really—the model as it progresses starts to generate longer and longer reasoning chains and that can get truncated and that can cause problems. There are a couple of different ways in which different papers have tried to tackle this. One is that either they increase the context length over the RL loop to handle that. In this particular case, they are basically putting this penalty function so that they can handle this part.

Overall, what this showed was that when you started with GRPO on this AIME benchmark, if you put overlong filtering, you went from 30 to 36 on the Qwen-32B model. If you did asymmetric clipping, you could go to 38. With software overlong punishment, you could go to 41, and then with token-level loss, you go to 42. And then if you do dynamic sampling where you're basically doing a better distribution of rewards across the batch, you have a better signal propagation. You basically got to 50, which was even better than what DeepSeek-R1 distilled down to Qwen-32B had done. They could actually hill-climb on the Qwen-32B themselves and get to a pretty strong score here.

Some of the learnings from this paper was that loss function by itself was not a reasonable enough proxy when you're learning in RL. You have to look at the response length, you have to look at the entropy and keep it so that it's not too low, but it's not too high either. And then the percentage of samples that have a full accuracy of one tells you how much you need to sample. All three are important principles in learning in RL. Some of the challenges that you might have to look out for is that if your response length in the model outputs is exploding, then you have to control the loss there, you have to control the entropy. And then if you're not seeing improvement after a certain number of steps, then your reward model actually might be saturated.

Effectively, what this is saying is that the optimization in the RL loop is harder. So if I were to answer the question that was brought up earlier of SFT versus RL: RL does provide you, in the domains where you do have a strong reward signal, the ability to hill-climb with fewer number of examples. But it does take a lot of work to get it right, while supervised fine-tuning, oftentimes if you do have access to a lot of high-quality data, is a faster way to just improve the model performance in certain cases, but it doesn't bring reasoning trace reasoning capabilities or boost them in certain ways.

If you were to compare the three different techniques that we talked about today: if you basically only had a few different examples, like a hundred examples with reasoning, and you are effectively just playing around with a model, STaR is a pretty good way—and you don't have auto infrastructure—STaR is a pretty good way for you to start getting the model to reason. Simple reasoning tasks like GSM8K actually will see a reasonable amount of improvement there, maybe not phenomenal, but you're still staying within what the model was capable of. If you go to DeepSeek-Math and you use GRPO, you do need a good enough base model plus you do need to prime the model with good instruction data. But now you have a reasonably strong algorithm that can work even if you don't have enough memory, you have a limited number of GPUs. For standard math reasoning tasks, this was well proven. And then you need DAPO-kind of techniques when your reasoning chains will end up being longer, which typically correlates with harder problems. If your reasoning chains are going to be much longer and you need state-of-the-art performance, then you do need to control all of these variables in your RL algorithm and infrastructure. For competition-level problems like AIME and IMO, this is very useful.

What is it that we expect to improve? All three techniques will improve in general the majority@k performance on the Y-axis as you put more compute. The answer formatting generally will improve as you apply these techniques. In general, the model will be more coherent over multiple steps. But none of these will yet improve the fundamental capability or just teach the model to solve new problems or generalize a lot out of domain. There is some set of questions that are being asked at this point in time: what will it take for the last part to start improving? Yes.

That's not the reason. I think the more fundamental question is that to improve pass@k, you're basically improving the fundamental capability of the model to solve new problems. You want to see the model show capabilities in certain ways. Typically the fundamental capability scaling comes from—the fundamental capability step jumps have typically been seen by either some sort of a breakthrough or by scaling in some dimension.

One of the things that I keep highlighting is something to keep in mind: a lot of the RL loop or even the STaR method—the quality of rationale, all of that depends on the ability of the model to reason or there needing to be some sort of a verifier or reward model. The reward models, if the model is too capable, then the rewards will get hacked. And if the reward model doesn't have enough signal, then you're basically not able to hill-climb the loop. So it is a harder optimization problem in that respect. We looked at last lecture or last Friday, we looked at the autonomous coding applications where you do have access to verifiable rewards because you have, like in math and code, the final correct answer. Then you can also give execution feedback, which was the RLEF paper we looked at. And then you also have your test execution. Similarly, there are some domains in which you have this kind of verification available. So it's easy for you to hill-climb, but then how many such places do you have this signal available? These questions will continue to come up. What verification signals are good enough? And then there's: can you use an ensemble of verifiers instead of one single verifier to make up for the gaps of what a single verifier will provide signal on? All of these questions are extremely important.

In terms of if you were to look at open problems in those domains, I think this is still an active—train time scaling is an active area of research. I already highlighted this. There's a fundamental question of why only majority@k increases, why this pass@k doesn't increase. A second question is that we do see in the reasoning chains things like backtracking or the model is able to self-evaluate, self-correct. We did show those examples early on, but are those behaviors the real—these reasoning behaviors—are they emerging or were they already present and they're basically statistically becoming more prevalent? And then a third bit right now is that we don't really have very good techniques to learn from failures. There have been a few papers that have tried to learn from failures in useful ways, but currently a lot of the techniques just filter them out.

Some promising directions that people can look to explore in this is along the data dimension of: how do you generate better data to train the model? Along the algorithm dimensions of: how do you get the reinforcement learning to be robust to noise in the reward models? What kind of rewards should we use? And then can you combine some of the rationalization techniques that we did with STaR with, say, the techniques that we did in DAPO? All of those are very interesting directions that you could look into for your projects. And with that, we have a few minutes left, but let's have questions.

So I think—like I don't think that's published for Anthropic, but I think Grok was more public. Typically the percentage of reinforcement learning versus pre-training has been closer to, I think last year was closer to, say, 99% to 1%, and then it's grown to perhaps like 5% or something like that. But Grok claimed that Grok-1 claimed that it did 50% RL, but it actually did not quite improve 50. Like 50% RL should give a big jump, but you're bottlenecked by a lot of the things that I'm showing you here, like your rewards are not strong enough or there is no ease in rewards. All of those things are going to be bottlenecks. So there is still a lot of open problems in this domain. Yeah. Let's see if there are more questions across the board. Is this actually too complex for people to ask questions? I apologize. Okay. Happy to answer questions after. More questions across the grass. No.

This is something like if you need a model for form or pay in this capital. And you help it. It's true for whom better than hell or part of it. A lot of them have always seen new jobs, but always very often you know, regardless of how well they put them on campus. You can never see them in your house. Like there's a fundamental reason. It's like fundamentally you can say you need a number in your house.

Yeah, it's basically able to explore better in the design space of what it knows. And by exploration and search, it's able to arrive at the solution.

There was a question here, which I didn't let you ask.

"All right. Yeah. I was curious for, especially on the directions of data. So for very unique datasets, like AIME or GSM8K, where the number of data is very limited, how can we ensure that we have enough signals for verifiable rewards? I mean, like there's not enough data, but we need a lot of signals. So how can you kind of make sure that you have enough training and verification without actually making data? So, you know, I mean, I was more data efficient. Do you want me to begin with? So, I mean, like that's why I was presenting these papers. You do see that sort of number of enough examples is going to be enough to hill-climb. The verification signals are more a function of having a good set of verifiers. So I think I thought we already covered the ensemble verifier paper, right? Like we were—she already covered that. So it's not just a matter of having a single verifier; you can have multiple verifiers."

All right. So when there's not enough data, we can just make up for that with many more verifiers.

I think there's all the data problems separately, but I don't think the "not enough data" is the right abstraction. I think it's: do you have enough data to hill-climb on? That is the right abstraction, right? And up-filtering out if you were doing dynamic sampling, for example. Okay. End of the class and I'm still around. Thanks, everyone.
